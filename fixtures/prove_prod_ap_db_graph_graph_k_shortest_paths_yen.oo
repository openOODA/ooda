// # Product prove std/db_graph/graph_k_shortest_paths_yen.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_graph/graph_k_shortest_paths_yen.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_graph/graph_k_shortest_paths_yen.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_graph_k_shortest_paths_yen: PASSED only if they match.

import "std/db_graph/graph_k_shortest_paths_yen.oo";

pub fn main() {
    let graph_k_shortest_paths_yen_a = ranked_path_new(0, 0.0);
    let graph_k_shortest_paths_yen_b = ranked_path_new(0, 0.0);
    if graph_k_shortest_paths_yen_a.rank != graph_k_shortest_paths_yen_b.rank {
        println("FAIL graph_k_shortest_paths_yen");
        process_exit(1);
    }
    println("prod_ap_graph_k_shortest_paths_yen: PASSED");
}
