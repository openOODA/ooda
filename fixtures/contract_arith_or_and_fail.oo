// M165: (a > 0 || b > 0) && c >= 0 fail-closed when c < 0
pub fn any_pos_nonneg(a: Int, b: Int, c: Int) -> Int
    requires (a > 0 || b > 0) && c >= 0
{
    return a + b + c;
}

pub fn main() {
    println(any_pos_nonneg(1, 0, -1));
}
