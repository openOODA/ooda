import "std/sec/crypto/sha3_ops.oo";

pub fn main() {
    let sha3_ops_mark: Int = 0;
    if sha3_ops_mark != 0 { process_exit(1); }
    let a: Int = sha3_checksum("x");
    let b: Int = sha3_checksum("x");
    if a != b { process_exit(1); }
    println("prod_sha3_ops: PASSED");
}
