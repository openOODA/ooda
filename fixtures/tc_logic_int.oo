// # Logical leftover needs Bool
//
// Logline: Check refuses && on Int leftover operands.
//
// Setup: Single-file probe. Line of the and stays under 50.
//
// Beats:
//   1. Use 1 && true as a println argument.
//   2. Do not bind an unused let.
//   3. check must ERR type logical operator requires Bool.

pub fn main() {
    println(1 && true);
}
