// # Product prove std/bft_consensus/state_sync_peer_scheduler.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/bft_consensus/state_sync_peer_scheduler.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/bft_consensus/state_sync_peer_scheduler.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_state_sync_peer_scheduler: PASSED only if they match.

import "std/bft_consensus/state_sync_peer_scheduler.oo";

pub fn main() {
    let state_sync_peer_scheduler_a = peer_scheduler_new(0);
    let state_sync_peer_scheduler_b = peer_scheduler_new(0);
    if state_sync_peer_scheduler_a.active_peers_count != state_sync_peer_scheduler_b.active_peers_count {
        println("FAIL state_sync_peer_scheduler");
        process_exit(1);
    }
    println("prod_ap_state_sync_peer_scheduler: PASSED");
}
