// # Product prove std/space_propulsion/b_plane_targeting_parameters.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/b_plane_targeting_parameters.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/b_plane_targeting_parameters.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_b_plane_targeting_parameters: PASSED only if they match.

import "std/space_propulsion/b_plane_targeting_parameters.oo";

pub fn main() {
    let b_plane_targeting_parameters_a = b_plane_target_spec_default_mars();
    let b_plane_targeting_parameters_b = b_plane_target_spec_default_mars();
    if b_plane_targeting_parameters_a.planet_mu != b_plane_targeting_parameters_b.planet_mu {
        println("FAIL b_plane_targeting_parameters");
        process_exit(1);
    }
    println("prod_ap_b_plane_targeting_parameters: PASSED");
}
