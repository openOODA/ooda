// # Product prove std/sec/sys/rop_engine.oo
//
// Logline: Fail-closed emit+check asserts __dummy_std_src_ooda_sec_sys_rop_engine_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/sys/rop_engine.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_sys_rop_engine_oo twice with no args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_rop_engine: PASSED only if they match.

import "std/sec/sys/rop_engine.oo";

pub fn main() {
    let rop_engine_mark: Int = 0;
    if rop_engine_mark != 0 { process_exit(1); }
    let a: Int = __dummy_std_src_ooda_sec_sys_rop_engine_oo();
    let b: Int = __dummy_std_src_ooda_sec_sys_rop_engine_oo();
    if a != b { process_exit(1); }
    println("prod_rop_engine: PASSED");
}
