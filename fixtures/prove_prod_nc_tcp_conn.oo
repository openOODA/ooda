// # Product prove std/net/tcp_conn.oo
//
// Logline: Fail-closed emit+gcc+run asserts tcp_conn_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/net/tcp_conn.oo.
//
// Beats:
//   1. Call tcp_conn_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_tcp_conn: PASSED only if it matches.

import "std/net/tcp_conn.oo";

pub fn main() {
    let a: Int = tcp_conn_probe();
    if a != 0 {
        println("FAIL tcp_conn_probe");
        process_exit(1);
    }
    println("prod_nc_tcp_conn: PASSED");
}
