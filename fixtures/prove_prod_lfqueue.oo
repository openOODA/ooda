// # Product sync lockfree queue prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sync/lockfree_queue.oo.
//
// Setup: Product std/sync/lockfree_queue.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. New len 0.
//   9. Exit 1 before PASSED on a miss.

import "std/sync/lockfree_queue.oo";

pub fn main() {
    let q: LockfreeQueue = lockfree_queue_new();
    let n: Int = lockfree_queue_len(q);
    if n != 0 {
        println("FAIL lfqueue 0");
        process_exit(1);
    }
    println("prod_lfqueue: PASSED");
}
