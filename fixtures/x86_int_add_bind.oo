// # emit-x86 bound int plus must not strcat
//
// Logline: Bind two ints and print a plus b so emit-x86 must fail closed instead of writing 12.
//
// Setup: Fold misses. No greet. a and b are const-int lets, not string lets.
//
// Beats:
//   1. Bind a to 1 and b to 2.
//   2. Print a plus b.
//   3. emit-x86 must print ERR x86_emit.
//
// residual: integer ALU stays later.

pub fn main() {
    let a = 1;
    let b = 2;
    println(a + b);
}
