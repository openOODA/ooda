// # Product prove std/physics_advanced/acoustic_pml_absorbing_boundary.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/physics_advanced/acoustic_pml_absorbing_boundary.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/physics_advanced/acoustic_pml_absorbing_boundary.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_acoustic_pml_absorbing_boundary: PASSED only if they match.

import "std/physics_advanced/acoustic_pml_absorbing_boundary.oo";

pub fn main() {
    let acoustic_pml_absorbing_boundary_a = pml_config_default(0.0, 0.0);
    let acoustic_pml_absorbing_boundary_b = pml_config_default(0.0, 0.0);
    if acoustic_pml_absorbing_boundary_a.pml_thickness != acoustic_pml_absorbing_boundary_b.pml_thickness {
        println("FAIL acoustic_pml_absorbing_boundary");
        process_exit(1);
    }
    println("prod_ap_acoustic_pml_absorbing_boundary: PASSED");
}
