// # Product prove std/hft_alpha/mm_hft_alpha_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts mock_is_test_environment is a real value.
//
// Setup: Isolated emit-c. Imports std/hft_alpha/mm_hft_alpha_stubs.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call mock_is_test_environment twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g0_mm_hft_alpha_stubs: PASSED only if they match.

import "std/hft_alpha/mm_hft_alpha_stubs.oo";

pub fn main() {
    let g0_a = mock_is_test_environment();
    let g0_b = mock_is_test_environment();
    if g0_a != g0_b {
        println("FAIL mock_is_test_environment");
        process_exit(1);
    }
    println("prod_g0_mm_hft_alpha_stubs: PASSED");
}
