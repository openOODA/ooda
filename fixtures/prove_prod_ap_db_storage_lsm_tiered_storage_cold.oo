// # Product prove std/db_storage/lsm_tiered_storage_cold.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/lsm_tiered_storage_cold.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/lsm_tiered_storage_cold.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_lsm_tiered_storage_cold: PASSED only if they match.

import "std/db_storage/lsm_tiered_storage_cold.oo";

pub fn main() {
    let lsm_tiered_storage_cold_a = tier_migration_policy_default();
    let lsm_tiered_storage_cold_b = tier_migration_policy_default();
    if lsm_tiered_storage_cold_a.hot_tier_max_bytes != lsm_tiered_storage_cold_b.hot_tier_max_bytes {
        println("FAIL lsm_tiered_storage_cold");
        process_exit(1);
    }
    println("prod_ap_lsm_tiered_storage_cold: PASSED");
}
