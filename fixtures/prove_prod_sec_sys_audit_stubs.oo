// # Product prove std/sec/sys/audit_stubs.oo
//
// Logline: Fail-closed emit+check asserts __dummy_std_src_ooda_sec_sys_audit_stubs_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/sys/audit_stubs.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_sys_audit_stubs_oo twice with no args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_audit_stubs: PASSED only if they match.

import "std/sec/sys/audit_stubs.oo";

pub fn main() {
    let audit_stubs_mark: Int = 0;
    if audit_stubs_mark != 0 { process_exit(1); }
    let a: Int = __dummy_std_src_ooda_sec_sys_audit_stubs_oo();
    let b: Int = __dummy_std_src_ooda_sec_sys_audit_stubs_oo();
    if a != b { process_exit(1); }
    println("prod_audit_stubs: PASSED");
}
