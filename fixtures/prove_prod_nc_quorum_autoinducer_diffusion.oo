// # Product prove std/synthetic_biology/quorum_autoinducer_diffusion.oo
//
// Logline: Fail-closed emit+gcc+run asserts quorum_autoinducer_diffusion_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/synthetic_biology/quorum_autoinducer_diffusion.oo.
//
// Beats:
//   1. Call quorum_autoinducer_diffusion_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_quorum_autoinducer_diffusion: PASSED only if it matches.

import "std/synthetic_biology/quorum_autoinducer_diffusion.oo";

pub fn main() {
    let a: Int = quorum_autoinducer_diffusion_probe();
    if a != 0 {
        println("FAIL quorum_autoinducer_diffusion_probe");
        process_exit(1);
    }
    println("prod_nc_quorum_autoinducer_diffusion: PASSED");
}
