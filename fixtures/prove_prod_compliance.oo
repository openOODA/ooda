// # Product prove std/enterprise/compliance.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/enterprise/compliance.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_compliance: PASSED (reference).

import "std/enterprise/compliance.oo";

pub fn main() {
    let compliance_mark: Int = 0;
    if compliance_mark != 0 {
        process_exit(1);
    }
    println("prod_compliance: PASSED (reference)");
}
