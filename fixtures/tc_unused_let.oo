// # Unused named let
//
// Logline: Check refuses a let that is never used.
//
// Setup: Single-file probe. Name is dead. Line stays under 50.
//
// Beats:
//   1. Bind dead to 1.
//   2. Print 0 without reading dead.
//   3. check must ERR unused let.

pub fn main() {
    let dead = 1;
    println(0);
}
