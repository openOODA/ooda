// M2: string list push + get + scope release (leak-safe free)
pub fn main() {
    let mut xs: List[String] = list_new();
    xs = list_push(xs, "a");
    xs = list_push(xs, "b");
    let s = list_get(xs, 0);
    let t = list_get(xs, 1);
    println(s);
    println(t);
}
