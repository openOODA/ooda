// ARC smoke: early return with live String locals
fn make(x: Int) -> String {
    let a: String = "a";
    let b: String = "b";
    if x > 0 {
        return a + b;
    }
    return b;
}
pub fn main(sys: &SysCap) {
    println(make(1));
    println(make(0));
}
