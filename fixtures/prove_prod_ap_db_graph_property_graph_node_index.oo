// # Product prove std/db_graph/property_graph_node_index.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_graph/property_graph_node_index.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_graph/property_graph_node_index.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_property_graph_node_index: PASSED only if they match.

import "std/db_graph/property_graph_node_index.oo";

pub fn main() {
    let property_graph_node_index_a = node_label_index_new(0);
    let property_graph_node_index_b = node_label_index_new(0);
    if property_graph_node_index_a.label_id != property_graph_node_index_b.label_id {
        println("FAIL property_graph_node_index");
        process_exit(1);
    }
    println("prod_ap_property_graph_node_index: PASSED");
}
