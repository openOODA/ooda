pub fn clamp(a: Int, b: Int, c: Int) -> Int
    requires a >= 0
    requires b >= 0
    requires c >= 0
    ensures result >= 0
{
    return a + b + c;
}
pub fn main() {
    println(clamp(1, -1, 3));
}
