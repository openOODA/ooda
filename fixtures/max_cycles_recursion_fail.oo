// MAX_CYCLES: 5
// M126: recursive calls debit shared fuel → exceed
fn burn(n: Int) -> Int {
    if n <= 0 {
        return 0;
    }
    return burn(n - 1);
}

pub fn main() {
    let x = burn(20);
    println(x);
}
