// # Product prove std/robot/locomotion_engine.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/robot/locomotion_engine.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_locomotion_engine: PASSED (reference).

import "std/robot/locomotion_engine.oo";

pub fn main() {
    let locomotion_engine_mark: Int = 0;
    if locomotion_engine_mark != 0 {
        process_exit(1);
    }
    println("prod_locomotion_engine: PASSED (reference)");
}
