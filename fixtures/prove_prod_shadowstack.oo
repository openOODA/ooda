import "std/sec/sys/shadowstack.oo";

pub fn main() {
    let shadowstack_mark: Int = 0;
    if shadowstack_mark != 0 { process_exit(1); }
    let a: Int = shim_marker_shadowstack_default_max_depth();
    let b: Int = shim_marker_shadowstack_default_max_depth();
    if a != b { process_exit(1); }
    println("prod_shadowstack: PASSED");
}
