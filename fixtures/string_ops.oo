// String subset: len, char_at, contains, str_slice (interp + WASM).
pub fn main() {
    let s = "hello";
    println(s.len());
    println(s.char_at(0));
    if s.contains("ell") {
        println(1);
    } else {
        println(0);
    }
    println(s.str_slice(1, 4));
}
