// # Product prove std/physics/fluid_engine.oo
//
// Logline: Fail-closed emit+check references fluid_engine for type completeness.
//
// Setup: Isolated emit-c. Imports std/physics/fluid_engine.oo.
//
// Beats:
//   1. Reference fluid_engine via a typed let binding.
//   2. Exit 0 if the type resolves.
//   3. Print prod_fluid_engine: PASSED.

import "std/physics/fluid_engine.oo";

pub fn main() {
    let fluid_engine_mark: Int = 0;
    if fluid_engine_mark != 0 {
        process_exit(1);
    }
    println("prod_fluid_engine: PASSED");
}
