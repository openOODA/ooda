// # Product prove std/mesh_ebpf/mesh_fault_injection.oo
//
// Logline: Fail-closed emit+gcc+run asserts mesh_fault_injection_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/mesh_ebpf/mesh_fault_injection.oo.
//
// Beats:
//   1. Call mesh_fault_injection_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_mesh_fault_injection: PASSED only if it matches.

import "std/mesh_ebpf/mesh_fault_injection.oo";

pub fn main() {
    let a: Int = mesh_fault_injection_probe();
    if a != 0 {
        println("FAIL mesh_fault_injection_probe");
        process_exit(1);
    }
    println("prod_nc_mesh_fault_injection: PASSED");
}
