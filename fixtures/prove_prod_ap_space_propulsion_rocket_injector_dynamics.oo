// # Product prove std/space_propulsion/rocket_injector_dynamics.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/rocket_injector_dynamics.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/rocket_injector_dynamics.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_rocket_injector_dynamics: PASSED only if they match.

import "std/space_propulsion/rocket_injector_dynamics.oo";

pub fn main() {
    let rocket_injector_dynamics_a = injector_coaxial_swirl_default();
    let rocket_injector_dynamics_b = injector_coaxial_swirl_default();
    if rocket_injector_dynamics_a.element_type != rocket_injector_dynamics_b.element_type {
        println("FAIL rocket_injector_dynamics");
        process_exit(1);
    }
    println("prod_ap_rocket_injector_dynamics: PASSED");
}
