// M113: function taking a secret propagates taint to result
// SECRET: api_key
pub fn do_something(key: String) -> String {
    return key;
}

pub fn main() {
    let api_key = "123";
    let y = do_something(api_key);
    println(y);
}
