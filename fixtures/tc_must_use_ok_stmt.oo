// # Unused Ok statement must fail check
//
// Logline: Check refuses Ok(1) used as a statement.
//
// Setup: Single-file probe. Line of Ok stays under 50.
//
// Beats:
//   1. Declare main.
//   2. Discard Ok(1) as a statement.
//   3. check must ERR unused Ok value (must-use).

pub fn main() {
    Ok(1);
}
