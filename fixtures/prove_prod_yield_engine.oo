// # Product prove std/fin/yield_engine.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/fin/yield_engine.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_yield_engine: PASSED (reference).

import "std/fin/yield_engine.oo";

pub fn main() {
    let yield_engine_mark: Int = 0;
    if yield_engine_mark != 0 {
        process_exit(1);
    }
    println("prod_yield_engine: PASSED (reference)");
}
