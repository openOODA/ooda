// # Product prove std/deep_ai/kv_cache_paged_manager.oo
//
// Logline: Fail-closed emit+gcc+run asserts kv_cache_paged_manager_marker is deterministic.
//
// Setup: Isolated emit-c. Imports std/deep_ai/kv_cache_paged_manager.oo.
//
// Beats:
//   1. Call kv_cache_paged_manager_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_kv_cache_paged_manager: PASSED only if they match.

import "std/deep_ai/kv_cache_paged_manager.oo";

pub fn main() {
    let a: Int = kv_cache_paged_manager_marker();
    let b: Int = kv_cache_paged_manager_marker();
    if a != b {
        println("FAIL kv_cache_paged_manager_marker");
        process_exit(1);
    }
    println("prod_kv_cache_paged_manager: PASSED");
}
