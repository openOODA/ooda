// # Product prove std/db_columnar/columnar_late_materialization.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_columnar/columnar_late_materialization.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_columnar/columnar_late_materialization.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_columnar_late_materialization: PASSED only if they match.

import "std/db_columnar/columnar_late_materialization.oo";

pub fn main() {
    let columnar_late_materialization_a = position_vector_new(0);
    let columnar_late_materialization_b = position_vector_new(0);
    if columnar_late_materialization_a.count != columnar_late_materialization_b.count {
        println("FAIL columnar_late_materialization");
        process_exit(1);
    }
    println("prod_ap_columnar_late_materialization: PASSED");
}
