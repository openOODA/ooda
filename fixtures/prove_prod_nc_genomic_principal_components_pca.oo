// # Product prove std/genomics_advanced/genomic_principal_components_pca.oo
//
// Logline: Fail-closed emit+gcc+run asserts genomic_principal_components_pca_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/genomics_advanced/genomic_principal_components_pca.oo.
//
// Beats:
//   1. Call genomic_principal_components_pca_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_genomic_principal_components_pca: PASSED only if it matches.

import "std/genomics_advanced/genomic_principal_components_pca.oo";

pub fn main() {
    let a: Int = genomic_principal_components_pca_probe();
    if a != 0 {
        println("FAIL genomic_principal_components_pca_probe");
        process_exit(1);
    }
    println("prod_nc_genomic_principal_components_pca: PASSED");
}
