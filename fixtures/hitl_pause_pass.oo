// # HITL pause absent
//
// Logline: Omit the HITL pause marker so check stays OK.
//
// Beats:
//   1. Do not emit a HITL pause line.
//   2. Leave main a println(0).
//   3. Check accepts the file.

// HITL path A: no pause marker → check OK
pub fn main() {
    println(0);
}
