// # Product prove std/os/sync.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/os/sync.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_sync: PASSED (reference).

import "std/os/sync.oo";

pub fn main() {
    let sync_mark: Int = 0;
    if sync_mark != 0 {
        process_exit(1);
    }
    println("prod_sync: PASSED (reference)");
}
