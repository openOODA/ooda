// # List arity-2 fuzz pass
//
// Logline: Return the first of two lists so the arity-2 list rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN list and target pick 0 6.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// M137 pure List arity-2 multi-arg fuzz pass
// FUZZ_DOMAIN: list
// FUZZ_TARGET: pick 0 6
// FUZZ_ENSURES: pick result == x
fn pick(x: List[Int], y: List[Int]) -> List[Int] {
    return x;
}

pub fn main() {
}
