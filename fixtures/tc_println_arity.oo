// # println needs one argument
//
// Logline: Check refuses println() with zero arguments.
//
// Setup: Single-file probe. Line of the call stays under 50.
//
// Beats:
//   1. Call println with no args.
//   2. Keep the call on a local line.
//   3. check must ERR type at that line.

pub fn main() {
    println();
}
