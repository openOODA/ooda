// # Product prove std/fintech_l3_book/fintech_l3_book_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts mock_itch_feed_new is deterministic.
//
// Setup: Isolated emit-c. Imports std/fintech_l3_book/fintech_l3_book_stubs.oo.
//
// Beats:
//   1. Call mock_itch_feed_new twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_fintech_l3_book_stubs: PASSED only if they match.

import "std/fintech_l3_book/fintech_l3_book_stubs.oo";

pub fn main() {
    let a: MockItchFeed = mock_itch_feed_new("x");
    let b: MockItchFeed = mock_itch_feed_new("x");
    if a.sequence_number != b.sequence_number {
        println("FAIL mock_itch_feed_new");
        process_exit(1);
    }
    println("prod_fintech_l3_book_stubs: PASSED");
}
