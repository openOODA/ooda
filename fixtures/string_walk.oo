// Alpha fixture: string walk via .len + .char_at (interp + WASM subset).
pub fn main() {
    let s = "ab";
    let mut i = 0;
    while i < s.len() {
        println(s.char_at(i));
        i = i + 1;
    }
}
