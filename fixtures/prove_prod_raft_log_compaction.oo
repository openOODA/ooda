// # Product prove std/cloud_k8s/raft_log_compaction.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/cloud_k8s/raft_log_compaction.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_raft_log_compaction: PASSED (reference).

import "std/cloud_k8s/raft_log_compaction.oo";

pub fn main() {
    let raft_log_compaction_mark: Int = 0;
    if raft_log_compaction_mark != 0 {
        process_exit(1);
    }
    println("prod_raft_log_compaction: PASSED (reference)");
}
