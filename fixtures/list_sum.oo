// List[Int] for-sum: method push + for desugar → list_get/list_len (interp + WASM).
pub fn main() {
    let mut xs: List[Int] = list_new();
    xs = xs.push(1);
    xs = xs.push(2);
    xs = xs.push(3);
    let mut s = 0;
    for x in xs {
        s = s + x;
    }
    println(s);
}
