// leftover: list_set on List[Node] stays ERR llvm_emit
struct Node { id: Int }
pub fn main() {
    let mut xs: List[Node] = list_new();
    let n: Node = Node { id: 1 };
    xs = list_push(xs, n);
    xs = list_set(xs, 0, n);
}
