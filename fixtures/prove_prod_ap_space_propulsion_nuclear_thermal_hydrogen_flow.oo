// # Product prove std/space_propulsion/nuclear_thermal_hydrogen_flow.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/nuclear_thermal_hydrogen_flow.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/nuclear_thermal_hydrogen_flow.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_nuclear_thermal_hydrogen_flow: PASSED only if they match.

import "std/space_propulsion/nuclear_thermal_hydrogen_flow.oo";

pub fn main() {
    let nuclear_thermal_hydrogen_flow_a = ntr_spec_default_nerva();
    let nuclear_thermal_hydrogen_flow_b = ntr_spec_default_nerva();
    if nuclear_thermal_hydrogen_flow_a.thermal_power_mw != nuclear_thermal_hydrogen_flow_b.thermal_power_mw {
        println("FAIL nuclear_thermal_hydrogen_flow");
        process_exit(1);
    }
    println("prod_ap_nuclear_thermal_hydrogen_flow: PASSED");
}
