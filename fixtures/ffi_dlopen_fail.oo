// # Bare dlopen refuse
//
// Logline: Call dlopen without UnsafeFFICap so check refuses the grant.
//
// Beats:
//   1. Define rogue with a bare dlopen.
//   2. Leave main empty.
//   3. Check fail-closes on the missing cap.

// Cap vs FFI path A: bare dlopen refuse
pub fn rogue() {
    let r = dlopen("libevil.so");
}
pub fn main() {}
