// M51 fail-at-runtime: second simple requires violated
pub fn both_pos(a: Int, b: Int) -> Int
    requires a >= 0
    requires b >= 0
{
    return a + b;
}

pub fn main() {
    println(both_pos(1, 0 - 2));
}
