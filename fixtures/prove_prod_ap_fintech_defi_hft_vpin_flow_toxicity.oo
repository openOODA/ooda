// # Product prove std/fintech_defi/hft_vpin_flow_toxicity.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/fintech_defi/hft_vpin_flow_toxicity.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/fintech_defi/hft_vpin_flow_toxicity.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_hft_vpin_flow_toxicity: PASSED only if they match.

import "std/fintech_defi/hft_vpin_flow_toxicity.oo";

pub fn main() {
    let hft_vpin_flow_toxicity_a = vpin_config_new(0, 0, 0);
    let hft_vpin_flow_toxicity_b = vpin_config_new(0, 0, 0);
    if hft_vpin_flow_toxicity_a.bucket_volume_size != hft_vpin_flow_toxicity_b.bucket_volume_size {
        println("FAIL hft_vpin_flow_toxicity");
        process_exit(1);
    }
    println("prod_ap_hft_vpin_flow_toxicity: PASSED");
}
