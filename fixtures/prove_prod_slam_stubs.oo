// # Product prove std/robot/slam_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/robot/slam_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_slam_stubs: PASSED (reference).

import "std/robot/slam_stubs.oo";

pub fn main() {
    let slam_stubs_mark: Int = 0;
    if slam_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_slam_stubs: PASSED (reference)");
}
