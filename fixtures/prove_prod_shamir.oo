import "std/sec/crypto/shamir.oo";

pub fn main() {
    let shamir_mark: Int = 0;
    if shamir_mark != 0 { process_exit(1); }
    let a: Int = shim_marker_checksum();
    let b: Int = shim_marker_checksum();
    if a != b { process_exit(1); }
    println("prod_shamir: PASSED");
}
