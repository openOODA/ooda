// M67 three simple requires + ensures AND
pub fn clamp(a: Int, b: Int, c: Int) -> Int
    requires a >= 0
    requires b >= 0
    requires c >= 0
    ensures result >= 0
    ensures result >= a
    ensures result >= b
{
    return a + b + c;
}
pub fn main() {
    println(clamp(1, 2, 3));
}
