import "std/sec/net/jwt_verify.oo";

pub fn main() {
    let jwt_verify_mark: Int = 0;
    if jwt_verify_mark != 0 { process_exit(1); }
    let a: Int = __dummy_std_src_ooda_sec_net_jwt_verify_oo();
    let b: Int = __dummy_std_src_ooda_sec_net_jwt_verify_oo();
    if a != b { process_exit(1); }
    println("prod_jwt_verify: PASSED");
}
