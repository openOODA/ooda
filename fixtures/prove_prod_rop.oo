import "std/sec/sys/rop.oo";

pub fn main() {
    let rop_mark: Int = 0;
    if rop_mark != 0 { process_exit(1); }
    let a: Int = shim_marker_gadget_type_pop_reg();
    let b: Int = shim_marker_gadget_type_pop_reg();
    if a != b { process_exit(1); }
    println("prod_rop: PASSED");
}
