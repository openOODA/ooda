// T1 if-taint clean: SECRET exists but if-branch rebinds only public literals
// SECRET: secret
pub fn main() {
    let secret = "token";
    let mut s = "public";
    if true {
        s = "clean";
    }
    println(s);
}
