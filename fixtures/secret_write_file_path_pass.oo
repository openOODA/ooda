// Secret allow non-secret: write_file_path
// SECRET: token
pub fn main(fs: &FsCap) {
    let token = "/tmp/sec";
    let path = "/tmp/ok";
    let content = "ok";
    let r = write_file(fs, path, content);
    println(1);
}
