// # Product prove std/space_propulsion/pulsed_plasma_thruster.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/pulsed_plasma_thruster.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/pulsed_plasma_thruster.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_pulsed_plasma_thruster: PASSED only if they match.

import "std/space_propulsion/pulsed_plasma_thruster.oo";

pub fn main() {
    let pulsed_plasma_thruster_a = ppt_circuit_spec_default();
    let pulsed_plasma_thruster_b = ppt_circuit_spec_default();
    if pulsed_plasma_thruster_a.capacitance_uf != pulsed_plasma_thruster_b.capacitance_uf {
        println("FAIL pulsed_plasma_thruster");
        process_exit(1);
    }
    println("prod_ap_pulsed_plasma_thruster: PASSED");
}
