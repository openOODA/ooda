// # Product prove std/bft_consensus/tendermint_evidence_slasher.oo
//
// Logline: Fail-closed emit+gcc+run asserts tendermint_evidence_slasher_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/bft_consensus/tendermint_evidence_slasher.oo.
//
// Beats:
//   1. Call tendermint_evidence_slasher_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_tendermint_evidence_slasher: PASSED only if it matches.

import "std/bft_consensus/tendermint_evidence_slasher.oo";

pub fn main() {
    let a: Int = tendermint_evidence_slasher_probe();
    if a != 0 {
        println("FAIL tendermint_evidence_slasher_probe");
        process_exit(1);
    }
    println("prod_nc_tendermint_evidence_slasher: PASSED");
}
