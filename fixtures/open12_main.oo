// # Per-file line numbers main
//
// Logline: Import the lib so check is multi-file.
//
// Setup: OPEN-12. Error must stay on the lib line, not a concat offset.
//
// Beats:
//   1. Import the sibling lib.
//   2. Print one.
//   3. Do not hide the unused Result.

import "open12_lib.oo";

pub fn main() {
    println(1);
}
