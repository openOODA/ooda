// M-? method-call return taint: chained call returns secret-derived value
// SECRET: api_key
pub fn make_secret() -> String {
    let api_key = "x";
    return api_key;
}
pub fn main() {
    let y = make_secret().len();
    println(y);
}
