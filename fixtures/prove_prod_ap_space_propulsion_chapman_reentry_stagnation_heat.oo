// # Product prove std/space_propulsion/chapman_reentry_stagnation_heat.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/chapman_reentry_stagnation_heat.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/chapman_reentry_stagnation_heat.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_chapman_reentry_stagnation_heat: PASSED only if they match.

import "std/space_propulsion/chapman_reentry_stagnation_heat.oo";

pub fn main() {
    let chapman_reentry_stagnation_heat_a = stagnation_spec_default_lunar_return();
    let chapman_reentry_stagnation_heat_b = stagnation_spec_default_lunar_return();
    if chapman_reentry_stagnation_heat_a.nose_radius_m != chapman_reentry_stagnation_heat_b.nose_radius_m {
        println("FAIL chapman_reentry_stagnation_heat");
        process_exit(1);
    }
    println("prod_ap_chapman_reentry_stagnation_heat: PASSED");
}
