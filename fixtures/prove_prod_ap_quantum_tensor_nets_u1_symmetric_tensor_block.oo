// # Product prove std/quantum_tensor_nets/u1_symmetric_tensor_block.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/u1_symmetric_tensor_block.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/u1_symmetric_tensor_block.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_u1_symmetric_tensor_block: PASSED only if they match.

import "std/quantum_tensor_nets/u1_symmetric_tensor_block.oo";

pub fn main() {
    let u1_symmetric_tensor_block_a = u1_charge_sector_new(0, 0);
    let u1_symmetric_tensor_block_b = u1_charge_sector_new(0, 0);
    if u1_symmetric_tensor_block_a.charge_q != u1_symmetric_tensor_block_b.charge_q {
        println("FAIL u1_symmetric_tensor_block");
        process_exit(1);
    }
    println("prod_ap_u1_symmetric_tensor_block: PASSED");
}
