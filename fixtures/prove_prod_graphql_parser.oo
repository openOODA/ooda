// # Product prove std/web_protocols/graphql_parser.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/web_protocols/graphql_parser.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_graphql_parser: PASSED (reference).

import "std/web_protocols/graphql_parser.oo";

pub fn main() {
    let graphql_parser_mark: Int = 0;
    if graphql_parser_mark != 0 {
        process_exit(1);
    }
    println("prod_graphql_parser: PASSED (reference)");
}
