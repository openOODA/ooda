// # Product prove std/grid/solar.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/grid/solar.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_solar: PASSED (reference).

import "std/grid/solar.oo";

pub fn main() {
    let solar_mark: Int = 0;
    if solar_mark != 0 {
        process_exit(1);
    }
    println("prod_solar: PASSED (reference)");
}
