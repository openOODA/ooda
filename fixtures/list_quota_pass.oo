// M114: List operations with explicit AllocCap bypass quota
pub fn main(alloc: &AllocCap) {
    let raw = alloc_bytes(alloc, 200000000);
    let mut lst: List[String] = list_new();
    let mut i = 0;
    while i < 5000000 {
        lst = list_push(lst, "a");
        i = i + 1;
    }
}
