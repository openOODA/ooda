// # Product prove std/physics_advanced/compressible_fanno_rayleigh_flow.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/physics_advanced/compressible_fanno_rayleigh_flow.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/physics_advanced/compressible_fanno_rayleigh_flow.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_compressible_fanno_rayleigh_flow: PASSED only if they match.

import "std/physics_advanced/compressible_fanno_rayleigh_flow.oo";

pub fn main() {
    let compressible_fanno_rayleigh_flow_a = fanno_evaluate_state(0.0, 0.0);
    let compressible_fanno_rayleigh_flow_b = fanno_evaluate_state(0.0, 0.0);
    if compressible_fanno_rayleigh_flow_a.mach != compressible_fanno_rayleigh_flow_b.mach {
        println("FAIL compressible_fanno_rayleigh_flow");
        process_exit(1);
    }
    println("prod_ap_compressible_fanno_rayleigh_flow: PASSED");
}
