// # Product prove std/pbr_raytracing/brdf_cook_torrance_ggx.oo
//
// Logline: Fail-closed emit+gcc+run asserts brdf_cook_torrance_ggx_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/pbr_raytracing/brdf_cook_torrance_ggx.oo.
//
// Beats:
//   1. Call brdf_cook_torrance_ggx_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_brdf_cook_torrance_ggx: PASSED only if it matches.

import "std/pbr_raytracing/brdf_cook_torrance_ggx.oo";

pub fn main() {
    let a: Int = brdf_cook_torrance_ggx_probe();
    if a != 0 {
        println("FAIL brdf_cook_torrance_ggx_probe");
        process_exit(1);
    }
    println("prod_nc_brdf_cook_torrance_ggx: PASSED");
}
