import "std/sec/crypto/sha3_stubs.oo";

pub fn main() {
    let sha3_stubs_mark: Int = 0;
    if sha3_stubs_mark != 0 { process_exit(1); }
    let a: List[Int] = keccak_f1600_permutation(0);
    let b: List[Int] = keccak_f1600_permutation(0);
    if a != b { process_exit(1); }
    println("prod_sha3_stubs: PASSED");
}
