// # Product prove std/space_propulsion/propellant_slosh_dynamics.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/propellant_slosh_dynamics.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/propellant_slosh_dynamics.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_propellant_slosh_dynamics: PASSED only if they match.

import "std/space_propulsion/propellant_slosh_dynamics.oo";

pub fn main() {
    let propellant_slosh_dynamics_a = tank_slosh_spec_default();
    let propellant_slosh_dynamics_b = tank_slosh_spec_default();
    if propellant_slosh_dynamics_a.tank_radius_m != propellant_slosh_dynamics_b.tank_radius_m {
        println("FAIL propellant_slosh_dynamics");
        process_exit(1);
    }
    println("prod_ap_propellant_slosh_dynamics: PASSED");
}
