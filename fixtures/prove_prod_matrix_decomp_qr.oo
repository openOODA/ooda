// # Product prove std/math/matrix_decomp_qr.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/math/matrix_decomp_qr.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_matrix_decomp_qr: PASSED (reference).

import "std/math/matrix_decomp_qr.oo";

pub fn main() {
    let matrix_decomp_qr_mark: Int = 0;
    if matrix_decomp_qr_mark != 0 {
        process_exit(1);
    }
    println("prod_matrix_decomp_qr: PASSED (reference)");
}
