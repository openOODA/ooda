// # Product prove std/exploit_analysis/kernel_krop_chain_builder.oo
//
// Logline: Fail-closed emit+gcc+run asserts kernel_krop_chain_builder_probe is deterministic.
//
// Setup: Isolated emit-c. Imports std/exploit_analysis/kernel_krop_chain_builder.oo.
//
// Beats:
//   1. Call kernel_krop_chain_builder_probe twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_kernel_krop_chain_builder: PASSED only if they match.

import "std/exploit_analysis/kernel_krop_chain_builder.oo";

pub fn main() {
    let a = kernel_krop_chain_builder_probe();
    let b = kernel_krop_chain_builder_probe();
    if a != b {
        println("FAIL kernel_krop_chain_builder_probe");
        process_exit(1);
    }
    println("prod_kernel_krop_chain_builder: PASSED");
}
