// # Product prove std/space_propulsion/ion_thruster_gridded_grid.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/ion_thruster_gridded_grid.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/ion_thruster_gridded_grid.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_ion_thruster_gridded_grid: PASSED only if they match.

import "std/space_propulsion/ion_thruster_gridded_grid.oo";

pub fn main() {
    let ion_thruster_gridded_grid_a = gridded_ion_spec_default_xenon();
    let ion_thruster_gridded_grid_b = gridded_ion_spec_default_xenon();
    if ion_thruster_gridded_grid_a.screen_voltage_v != ion_thruster_gridded_grid_b.screen_voltage_v {
        println("FAIL ion_thruster_gridded_grid");
        process_exit(1);
    }
    println("prod_ap_ion_thruster_gridded_grid: PASSED");
}
