// # Unused named import
//
// Logline: Check refuses an import that is never used.
//
// Setup: Single-file probe. Path is the unused lib. Line stays under 50.
//
// Beats:
//   1. Import the unused lib.
//   2. Print 0 without calling the lib.
//   3. check must ERR unused import.

import "fixtures/unused_import_lib.oo";

pub fn main() {
    println(0);
}
