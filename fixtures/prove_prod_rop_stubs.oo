// # Product prove std/sec/sys/rop_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts __dummy_std_src_ooda_sec_sys_rop_stubs_oo real value.
//
// Setup: Isolated emit-c. Imports std/sec/sys/rop_stubs.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_sys_rop_stubs_oo with a known case.
//   2. Exit 1 on miss.
//   3. Print prod_rop_stubs: PASSED only if the assert holds.

import "std/sec/sys/rop_stubs.oo";

pub fn main() {
    let rop_stubs_mark: Int = 0;
    if rop_stubs_mark != 0 {
        process_exit(1);
    }
    let rop_stubs_n: Int = __dummy_std_src_ooda_sec_sys_rop_stubs_oo();
    if rop_stubs_n != 0 {
        println("FAIL __dummy_std_src_ooda_sec_sys_rop_stubs_oo");
        process_exit(1);
    }
    println("prod_rop_stubs: PASSED");
}
