// M-? field clean: struct field stays clean, field read at sink
type Box = struct { v: String };
// SECRET: api_key
pub fn main() {
    let api_key = "x";
    let b = Box { v: "public" };
    println(b.v);
}
