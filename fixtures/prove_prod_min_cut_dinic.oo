// # Product prove std/graph_topology/min_cut_dinic.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/graph_topology/min_cut_dinic.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_min_cut_dinic: PASSED (reference).

import "std/graph_topology/min_cut_dinic.oo";

pub fn main() {
    let min_cut_dinic_mark: Int = 0;
    if min_cut_dinic_mark != 0 {
        process_exit(1);
    }
    println("prod_min_cut_dinic: PASSED (reference)");
}
