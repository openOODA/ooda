// # Product prove std/genomics_advanced/read_backed_phasing_whatshap.oo
//
// Logline: Fail-closed emit+gcc+run asserts read_backed_phasing_whatshap_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/genomics_advanced/read_backed_phasing_whatshap.oo.
//
// Beats:
//   1. Call read_backed_phasing_whatshap_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_read_backed_phasing_whatshap: PASSED only if it matches.

import "std/genomics_advanced/read_backed_phasing_whatshap.oo";

pub fn main() {
    let a: Int = read_backed_phasing_whatshap_probe();
    if a != 0 {
        println("FAIL read_backed_phasing_whatshap_probe");
        process_exit(1);
    }
    println("prod_nc_read_backed_phasing_whatshap: PASSED");
}
