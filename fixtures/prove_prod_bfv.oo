import "std/sec/crypto/bfv.oo";

pub fn main() {
    let bfv_mark: Int = 0;
    if bfv_mark != 0 { process_exit(1); }
    let a: Int = shim_marker_checksum();
    let b: Int = shim_marker_checksum();
    if a != b { process_exit(1); }
    println("prod_bfv: PASSED");
}
