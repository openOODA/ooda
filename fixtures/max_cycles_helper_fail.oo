// M92 MaxCycles on helper fn (shared per-fn, not main)
// MAX_CYCLES: 3
fn burn(n: Int) -> Int {
    let mut i = 0;
    while i < n {
        i = i + 1;
    }
    return i;
}
pub fn main() {
    println(burn(10));
}
