// # Product prove std/space_propulsion/patched_conics_flyby.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/patched_conics_flyby.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/patched_conics_flyby.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_patched_conics_flyby: PASSED only if they match.

import "std/space_propulsion/patched_conics_flyby.oo";

pub fn main() {
    let patched_conics_flyby_a = flyby_spec_default_venus();
    let patched_conics_flyby_b = flyby_spec_default_venus();
    if patched_conics_flyby_a.planet_mu != patched_conics_flyby_b.planet_mu {
        println("FAIL patched_conics_flyby");
        process_exit(1);
    }
    println("prod_ap_patched_conics_flyby: PASSED");
}
