// # Product prove std/formal_verif/sat_cdcl_core_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts sat_cdcl_core_engine_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/formal_verif/sat_cdcl_core_engine.oo.
//
// Beats:
//   1. Call sat_cdcl_core_engine_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_sat_cdcl_core_engine: PASSED only if it matches.

import "std/formal_verif/sat_cdcl_core_engine.oo";

pub fn main() {
    let a: Int = sat_cdcl_core_engine_probe();
    if a != 0 {
        println("FAIL sat_cdcl_core_engine_probe");
        process_exit(1);
    }
    println("prod_nc_sat_cdcl_core_engine: PASSED");
}
