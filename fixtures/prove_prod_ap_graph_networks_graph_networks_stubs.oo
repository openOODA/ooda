// # Product prove std/graph_networks/graph_networks_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/graph_networks/graph_networks_stubs.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/graph_networks/graph_networks_stubs.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_graph_networks_stubs: PASSED only if they match.

import "std/graph_networks/graph_networks_stubs.oo";

pub fn main() {
    let graph_networks_stubs_a = graph_networks_accel_init(0);
    let graph_networks_stubs_b = graph_networks_accel_init(0);
    if graph_networks_stubs_a.device_id != graph_networks_stubs_b.device_id {
        println("FAIL graph_networks_stubs");
        process_exit(1);
    }
    println("prod_ap_graph_networks_stubs: PASSED");
}
