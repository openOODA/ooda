// # Product prove std/db_storage/buffer_pool_clock_sweep.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/buffer_pool_clock_sweep.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/buffer_pool_clock_sweep.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_buffer_pool_clock_sweep: PASSED only if they match.

import "std/db_storage/buffer_pool_clock_sweep.oo";

pub fn main() {
    let buffer_pool_clock_sweep_a = clock_frame_new(0, 0);
    let buffer_pool_clock_sweep_b = clock_frame_new(0, 0);
    if buffer_pool_clock_sweep_a.frame_id != buffer_pool_clock_sweep_b.frame_id {
        println("FAIL buffer_pool_clock_sweep");
        process_exit(1);
    }
    println("prod_ap_buffer_pool_clock_sweep: PASSED");
}
