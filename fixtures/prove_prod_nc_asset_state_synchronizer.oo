// # Product prove std/digital_twin/asset_state_synchronizer.oo
//
// Logline: Fail-closed emit+gcc+run asserts asset_state_synchronizer_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/digital_twin/asset_state_synchronizer.oo.
//
// Beats:
//   1. Call asset_state_synchronizer_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_asset_state_synchronizer: PASSED only if it matches.

import "std/digital_twin/asset_state_synchronizer.oo";

pub fn main() {
    let a: Int = asset_state_synchronizer_probe();
    if a != 0 {
        println("FAIL asset_state_synchronizer_probe");
        process_exit(1);
    }
    println("prod_nc_asset_state_synchronizer: PASSED");
}
