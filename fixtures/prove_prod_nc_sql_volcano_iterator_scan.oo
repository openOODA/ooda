// # Product prove std/db_storage/sql_volcano_iterator_scan.oo
//
// Logline: Fail-closed emit+gcc+run asserts sql_volcano_iterator_scan_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/db_storage/sql_volcano_iterator_scan.oo.
//
// Beats:
//   1. Call sql_volcano_iterator_scan_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_sql_volcano_iterator_scan: PASSED only if it matches.

import "std/db_storage/sql_volcano_iterator_scan.oo";

pub fn main() {
    let a: Int = sql_volcano_iterator_scan_probe();
    if a != 0 {
        println("FAIL sql_volcano_iterator_scan_probe");
        process_exit(1);
    }
    println("prod_nc_sql_volcano_iterator_scan: PASSED");
}
