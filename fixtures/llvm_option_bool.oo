// # Unknown Option shape refuse
//
// Logline: emit-llvm fail-closes Option[Bool] rather than inventing a compound.
//
// Setup: Backend-LLVM. Not a run lock.
//
// Beats:
//   1. Annotate Option[Bool].
//   2. Assign Some(true).
//   3. Emit must print ERR llvm_emit.

pub fn main() {
    let a: Option[Bool] = Some(true);
    println(a.is_ok().to_string());
}
