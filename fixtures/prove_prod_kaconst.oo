// # Product sec kernel audit const prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sec/sys/kernel_audit_const.oo.
//
// Setup: Product std/sec/sys/kernel_audit_const.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. perm exec 1 read 4. action always 1.
//   9. Exit 1 before PASSED on a miss.

import "std/sec/sys/kernel_audit_const.oo";

pub fn main() {
    let a: Int = audit_perm_exec();
    if a != 1 {
        println("FAIL ka exec");
        process_exit(1);
    }
    let b: Int = audit_perm_read();
    if b != 4 {
        println("FAIL ka read");
        process_exit(1);
    }
    let c: Int = audit_action_always();
    if c != 1 {
        println("FAIL ka always");
        process_exit(1);
    }
    println("prod_kaconst: PASSED");
}
