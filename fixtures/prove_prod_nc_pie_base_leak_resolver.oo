// # Product prove std/exploit_analysis/pie_base_leak_resolver.oo
//
// Logline: Fail-closed emit+gcc+run asserts pie_base_leak_resolver_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/exploit_analysis/pie_base_leak_resolver.oo.
//
// Beats:
//   1. Call pie_base_leak_resolver_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_pie_base_leak_resolver: PASSED only if it matches.

import "std/exploit_analysis/pie_base_leak_resolver.oo";

pub fn main() {
    let a: Int = pie_base_leak_resolver_probe();
    if a != 0 {
        println("FAIL pie_base_leak_resolver_probe");
        process_exit(1);
    }
    println("prod_nc_pie_base_leak_resolver: PASSED");
}
