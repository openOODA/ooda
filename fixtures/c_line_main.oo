// # Entry file for multi-file #line
//
// Logline: Import c_line_lib and print two() so default emit-c names both paths.
//
// Setup: Default emit-c (no EMIT_NO_CONCAT). Backend-C.
//
// Beats:
//   1. Import the lib.
//   2. Call two in main.
//   3. Print the Int.

import "c_line_lib.oo";

pub fn main() {
    let n: Int = two();
    println(n);
}
