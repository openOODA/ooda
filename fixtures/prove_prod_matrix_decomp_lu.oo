// # Product prove std/math/matrix_decomp_lu.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/math/matrix_decomp_lu.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_matrix_decomp_lu: PASSED (reference).

import "std/math/matrix_decomp_lu.oo";

pub fn main() {
    let matrix_decomp_lu_mark: Int = 0;
    if matrix_decomp_lu_mark != 0 {
        process_exit(1);
    }
    println("prod_matrix_decomp_lu: PASSED (reference)");
}
