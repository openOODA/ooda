// # Product prove std/compilers_polyhedral/autovec_interleaved_access.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/compilers_polyhedral/autovec_interleaved_access.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/compilers_polyhedral/autovec_interleaved_access.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_autovec_interleaved_access: PASSED only if they match.

import "std/compilers_polyhedral/autovec_interleaved_access.oo";

pub fn main() {
    let autovec_interleaved_access_a = compute_shuffle_overhead(0, 0);
    let autovec_interleaved_access_b = compute_shuffle_overhead(0, 0);
    if autovec_interleaved_access_a != autovec_interleaved_access_b {
        println("FAIL autovec_interleaved_access");
        process_exit(1);
    }
    println("prod_ap_autovec_interleaved_access: PASSED");
}
