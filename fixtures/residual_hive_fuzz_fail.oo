// # Call residual hive_fuzz
//
// Logline: A bare call to hive_fuzz is the residual fail rail for that surface.
//
// Beats:
//   1. Invoke the residual name from main.
//   2. Bind the result so the call is live.
//   3. Expect check or emit to refuse the residual.

pub fn main() { let r = hive_fuzz(1); }
