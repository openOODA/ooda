// # Product prove std/nerf_3dgs/camera_ray_bundle_generator.oo
//
// Logline: Fail-closed emit+gcc+run asserts camera_ray_bundle_generator_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/nerf_3dgs/camera_ray_bundle_generator.oo.
//
// Beats:
//   1. Call camera_ray_bundle_generator_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_camera_ray_bundle_generator: PASSED only if it matches.

import "std/nerf_3dgs/camera_ray_bundle_generator.oo";

pub fn main() {
    let a: Int = camera_ray_bundle_generator_probe();
    if a != 0 {
        println("FAIL camera_ray_bundle_generator_probe");
        process_exit(1);
    }
    println("prod_nc_camera_ray_bundle_generator: PASSED");
}
