// M128: Secret content must not reach write_file
// SECRET: token
pub fn main(fs: &FsCap) {
    let token = "s3cret";
    let path = "/tmp/ooda_secret_sink_should_not_write";
    let r = write_file(fs, path, token);
    println(1);
}
