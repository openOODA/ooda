// # Product prove std/sec/net/jwt_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/sec/net/jwt_engine.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/net/jwt_engine.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_jwt_engine: PASSED only if they match.

import "std/sec/net/jwt_engine.oo";

pub fn main() {
    let jwt_engine_a = __dummy_std_src_ooda_sec_net_jwt_engine_oo();
    let jwt_engine_b = __dummy_std_src_ooda_sec_net_jwt_engine_oo();
    if jwt_engine_a != jwt_engine_b {
        println("FAIL jwt_engine");
        process_exit(1);
    }
    println("prod_ap_jwt_engine: PASSED");
}
