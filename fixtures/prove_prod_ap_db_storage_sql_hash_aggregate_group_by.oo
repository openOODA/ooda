// # Product prove std/db_storage/sql_hash_aggregate_group_by.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/sql_hash_aggregate_group_by.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/sql_hash_aggregate_group_by.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_sql_hash_aggregate_group_by: PASSED only if they match.

import "std/db_storage/sql_hash_aggregate_group_by.oo";

pub fn main() {
    let sql_hash_aggregate_group_by_a = aggregate_accumulator_new();
    let sql_hash_aggregate_group_by_b = aggregate_accumulator_new();
    if sql_hash_aggregate_group_by_a.count != sql_hash_aggregate_group_by_b.count {
        println("FAIL sql_hash_aggregate_group_by");
        process_exit(1);
    }
    println("prod_ap_sql_hash_aggregate_group_by: PASSED");
}
