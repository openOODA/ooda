// # Product prove std/net/pay_offer.oo
//
// Logline: Fail-closed emit+gcc+run asserts pay_offer_header_name is deterministic.
//
// Setup: Isolated emit-c. Imports std/net/pay_offer.oo.
//
// Beats:
//   1. Call pay_offer_header_name twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_pay_offer: PASSED only if they match.

import "std/net/pay_offer.oo";

pub fn main() {
    let a = pay_offer_header_name();
    let b = pay_offer_header_name();
    if a != b {
        println("FAIL pay_offer_header_name");
        process_exit(1);
    }
    println("prod_pay_offer: PASSED");
}
