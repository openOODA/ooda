// # String order compare is not numeric
//
// Logline: Check refuses "a" < "b" as an ordered compare.
//
// Setup: Single-file probe. Line of the compare stays under 50.
//
// Beats:
//   1. Compare two string literals with <.
//   2. Keep the compare in main.
//   3. check must ERR type comparison requires matching numeric.

pub fn main() {
    if "a" < "b" {
        println(1);
    }
}
