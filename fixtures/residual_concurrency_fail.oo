// # Call actor_spawn without ThreadCap
//
// Logline: A bare actor_spawn without ThreadCap still fail-closes at check.
//
// Beats:
//   1. Call actor_spawn with a string name and no cap.
//   2. Keep the historical residual fixture name.
//   3. Prefer actor_path_a.oo for the product path-A rail.

// historical residual fixture name — M165: actor_spawn sealed under ThreadCap (not residual refuse)
// Bare call without ThreadCap still fail-closed at check (capability).
// Prefer fixtures/actor_path_a.oo for path A rails.
pub fn main() { let r = actor_spawn("x"); }
