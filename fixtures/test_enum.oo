type Value = Num(Int) | Flag(Bool) | Empty;

pub fn main() {
    let e1: Value = Num(55);
    let r1: Int = match e1 {
        Num(n) => n,
        Flag(b) => 0,
        Empty => 0
    };
    assert_eq(r1, 55);
    println("PASS");
}
