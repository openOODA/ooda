// # Discarded Ok must refuse if DE-MUST landed
//
// Logline: Bare Ok(1) as a statement is unused Result.
//
// Setup: Isolated check. No bind, no return.
//
// Beats:
//   1. Open main.
//   2. Discard Ok(1).
//   3. check must ERR must-use if DE-MUST is live.

pub fn main() {
    Ok(1);
}
