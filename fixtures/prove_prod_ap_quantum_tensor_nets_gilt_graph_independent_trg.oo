// # Product prove std/quantum_tensor_nets/gilt_graph_independent_trg.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/gilt_graph_independent_trg.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/gilt_graph_independent_trg.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_gilt_graph_independent_trg: PASSED only if they match.

import "std/quantum_tensor_nets/gilt_graph_independent_trg.oo";

pub fn main() {
    let gilt_graph_independent_trg_a = gilt_config_default();
    let gilt_graph_independent_trg_b = gilt_config_default();
    if gilt_graph_independent_trg_a.filter_epsilon != gilt_graph_independent_trg_b.filter_epsilon {
        println("FAIL gilt_graph_independent_trg");
        process_exit(1);
    }
    println("prod_ap_gilt_graph_independent_trg: PASSED");
}
