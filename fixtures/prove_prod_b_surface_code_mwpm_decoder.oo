// # Product prove std/quantum_advanced/surface_code_mwpm_decoder.oo
//
// Logline: Fail-closed emit+gcc+run asserts surface_code_mwpm_decoder_probe is deterministic.
//
// Setup: Isolated emit-c. Imports std/quantum_advanced/surface_code_mwpm_decoder.oo.
//
// Beats:
//   1. Call surface_code_mwpm_decoder_probe twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_surface_code_mwpm_decoder: PASSED only if they match.

import "std/quantum_advanced/surface_code_mwpm_decoder.oo";

pub fn main() {
    let a = surface_code_mwpm_decoder_probe();
    let b = surface_code_mwpm_decoder_probe();
    if a != b {
        println("FAIL surface_code_mwpm_decoder_probe");
        process_exit(1);
    }
    println("prod_surface_code_mwpm_decoder: PASSED");
}
