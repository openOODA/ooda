// # Product prove std/os/memory_mapped_stub.oo
//
// Logline: Fail-closed emit+gcc+run asserts memory_mapped_stub_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/os/memory_mapped_stub.oo.
//
// Beats:
//   1. Call memory_mapped_stub_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_memory_mapped_stub: PASSED only if it matches.

import "std/os/memory_mapped_stub.oo";

pub fn main() {
    let a: Int = memory_mapped_stub_probe();
    if a != 0 {
        println("FAIL memory_mapped_stub_probe");
        process_exit(1);
    }
    println("prod_nc_memory_mapped_stub: PASSED");
}
