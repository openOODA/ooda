// # Product prove std/bft_consensus/tendermint_abci_application_interface.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/bft_consensus/tendermint_abci_application_interface.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/bft_consensus/tendermint_abci_application_interface.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_tendermint_abci_application_interface: PASSED only if they match.

import "std/bft_consensus/tendermint_abci_application_interface.oo";

pub fn main() {
    let tendermint_abci_application_interface_a = abci_process_proposal_accept();
    let tendermint_abci_application_interface_b = abci_process_proposal_accept();
    if tendermint_abci_application_interface_a.is_accepted != tendermint_abci_application_interface_b.is_accepted {
        println("FAIL tendermint_abci_application_interface");
        process_exit(1);
    }
    println("prod_ap_tendermint_abci_application_interface: PASSED");
}
