// # Product prove std/crispr_cas/off_target_guide_seq_validation.oo
//
// Logline: Fail-closed emit+gcc+run asserts off_target_guide_seq_validation_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/off_target_guide_seq_validation.oo.
//
// Beats:
//   1. Call off_target_guide_seq_validation_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_off_target_guide_seq_validation: PASSED only if it matches.

import "std/crispr_cas/off_target_guide_seq_validation.oo";

pub fn main() {
    let a: Int = off_target_guide_seq_validation_probe();
    if a != 0 {
        println("FAIL off_target_guide_seq_validation_probe");
        process_exit(1);
    }
    println("prod_nc_off_target_guide_seq_validation: PASSED");
}
