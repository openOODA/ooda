// # Product prove std/auto_mpc_control/mpc_adaptive_weight_tuning.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/auto_mpc_control/mpc_adaptive_weight_tuning.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/auto_mpc_control/mpc_adaptive_weight_tuning.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_mpc_adaptive_weight_tuning: PASSED only if they match.

import "std/auto_mpc_control/mpc_adaptive_weight_tuning.oo";

pub fn main() {
    let mpc_adaptive_weight_tuning_a = compute_adaptive_weights(0.0, 0.0, 0.0, true);
    let mpc_adaptive_weight_tuning_b = compute_adaptive_weights(0.0, 0.0, 0.0, true);
    if mpc_adaptive_weight_tuning_a.weight_cte != mpc_adaptive_weight_tuning_b.weight_cte {
        println("FAIL mpc_adaptive_weight_tuning");
        process_exit(1);
    }
    println("prod_ap_mpc_adaptive_weight_tuning: PASSED");
}
