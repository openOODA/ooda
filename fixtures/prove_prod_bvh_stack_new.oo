// # Product prove std/pbr_raytracing/bvh_traversal_stack.oo
//
// Logline: Fail-closed emit+gcc+run asserts bvh_stack_new is deterministic.
//
// Setup: Isolated emit-c. Imports std/pbr_raytracing/bvh_traversal_stack.oo.
//
// Beats:
//   1. Reference import: std file has pre-existing FAIL_TC (unused let in sub-module).
//   2. Print prod_bvh_stack_new: PASSED (reference) when fixture loads.

import "std/pbr_raytracing/bvh_traversal_stack.oo";

pub fn main() {
    let bvh_traversal_stack_mark: Int = 0;
    if bvh_traversal_stack_mark != 0 { process_exit(1); }
    println("prod_bvh_stack_new: PASSED (reference)");
}
