// # Product prove std/physics/collision_engine.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/physics/collision_engine.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_collision_engine: PASSED (reference).

import "std/physics/collision_engine.oo";

pub fn main() {
    let collision_engine_mark: Int = 0;
    if collision_engine_mark != 0 {
        process_exit(1);
    }
    println("prod_collision_engine: PASSED (reference)");
}
