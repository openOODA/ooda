// # Product prove std/db_storage/buffer_pool_frame_manager.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/buffer_pool_frame_manager.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/buffer_pool_frame_manager.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_buffer_pool_frame_manager: PASSED only if they match.

import "std/db_storage/buffer_pool_frame_manager.oo";

pub fn main() {
    let buffer_pool_frame_manager_a = buffer_pool_manager_new(0);
    let buffer_pool_frame_manager_b = buffer_pool_manager_new(0);
    if buffer_pool_frame_manager_a.pool_size != buffer_pool_frame_manager_b.pool_size {
        println("FAIL buffer_pool_frame_manager");
        process_exit(1);
    }
    println("prod_ap_buffer_pool_frame_manager: PASSED");
}
