// # Product prove std/compilers_polyhedral/polyhedral_temporal_locality.oo
//
// Logline: Fail-closed emit+gcc+run asserts calculate_optimal_statement_shift is a real value.
//
// Setup: Isolated emit-c. Imports std/compilers_polyhedral/polyhedral_temporal_locality.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call calculate_optimal_statement_shift twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g0_polyhedral_temporal_locality: PASSED only if they match.

import "std/compilers_polyhedral/polyhedral_temporal_locality.oo";

pub fn main() {
    let g0_a = calculate_optimal_statement_shift(2);
    let g0_b = calculate_optimal_statement_shift(2);
    if g0_a != g0_b {
        println("FAIL calculate_optimal_statement_shift");
        process_exit(1);
    }
    println("prod_g0_polyhedral_temporal_locality: PASSED");
}
