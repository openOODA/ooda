// # Product prove std/sec/net/jwt_payload.oo
//
// Logline: Fail-closed emit+gcc+run asserts __dummy_std_src_ooda_sec_net_jwt_payload_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/net/jwt_payload.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_net_jwt_payload_oo twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_jwt_payload: PASSED only if they match.

import "std/sec/net/jwt_payload.oo";

pub fn main() {
    let a = __dummy_std_src_ooda_sec_net_jwt_payload_oo();
    let b = __dummy_std_src_ooda_sec_net_jwt_payload_oo();
    if a != b {
        println("FAIL __dummy_std_src_ooda_sec_net_jwt_payload_oo");
        process_exit(1);
    }
    println("prod_jwt_payload: PASSED");
}
