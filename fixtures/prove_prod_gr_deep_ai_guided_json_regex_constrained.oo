// # Product prove std/deep_ai/guided_json_regex_constrained.oo
//
// Logline: Fail-closed emit+gcc+run asserts guided_json_regex_constrained_marker is a real value.
//
// Setup: Isolated emit-c. Imports std/deep_ai/guided_json_regex_constrained.oo. Avoids String != and Result unwrap.
//
// Beats:
//   1. Call guided_json_regex_constrained_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_gr_guided_json_regex_constrained: PASSED only if they match.

import "std/deep_ai/guided_json_regex_constrained.oo";

pub fn main() {
    let g0_a = guided_json_regex_constrained_marker();
    let g0_b = guided_json_regex_constrained_marker();
    if g0_a != g0_b {
        println("FAIL guided_json_regex_constrained_marker");
        process_exit(1);
    }
    println("prod_gr_guided_json_regex_constrained: PASSED");
}
