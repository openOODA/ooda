// # Bind quote escape literals
//
// Logline: Bind quote, tab, newline, and backslash literals and print the quote.
//
// Setup: Test fixture. Keep all code exact.
//
// Beats:
//   1. Bind the four escape strings.
//   2. Leave tab, newline, and slash unused.
//   3. Print the quote.
//
pub fn main() {
    let q = "\"";
    let t = "\t";
    let n = "\n";
    let s = "\\";
    println(q);
}
