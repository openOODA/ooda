// # Product prove std/crispr_cas/prime_editing_rtt_designer.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/crispr_cas/prime_editing_rtt_designer.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/prime_editing_rtt_designer.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_prime_editing_rtt_designer: PASSED only if they match.

import "std/crispr_cas/prime_editing_rtt_designer.oo";

pub fn main() {
    let prime_editing_rtt_designer_a = rtt_evaluate_spec("x", 0, true);
    let prime_editing_rtt_designer_b = rtt_evaluate_spec("x", 0, true);
    if prime_editing_rtt_designer_a.rtt_seq != prime_editing_rtt_designer_b.rtt_seq {
        println("FAIL prime_editing_rtt_designer");
        process_exit(1);
    }
    println("prod_ap_prime_editing_rtt_designer: PASSED");
}
