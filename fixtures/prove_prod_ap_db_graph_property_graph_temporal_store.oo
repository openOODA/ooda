// # Product prove std/db_graph/property_graph_temporal_store.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_graph/property_graph_temporal_store.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_graph/property_graph_temporal_store.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_property_graph_temporal_store: PASSED only if they match.

import "std/db_graph/property_graph_temporal_store.oo";

pub fn main() {
    let property_graph_temporal_store_a = time_interval_new(0, 0);
    let property_graph_temporal_store_b = time_interval_new(0, 0);
    if property_graph_temporal_store_a.start_ts != property_graph_temporal_store_b.start_ts {
        println("FAIL property_graph_temporal_store");
        process_exit(1);
    }
    println("prod_ap_property_graph_temporal_store: PASSED");
}
