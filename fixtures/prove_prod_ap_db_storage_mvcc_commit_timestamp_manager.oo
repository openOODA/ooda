// # Product prove std/db_storage/mvcc_commit_timestamp_manager.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/mvcc_commit_timestamp_manager.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/mvcc_commit_timestamp_manager.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_mvcc_commit_timestamp_manager: PASSED only if they match.

import "std/db_storage/mvcc_commit_timestamp_manager.oo";

pub fn main() {
    let mvcc_commit_timestamp_manager_a = hlc_timestamp_new(0, 0);
    let mvcc_commit_timestamp_manager_b = hlc_timestamp_new(0, 0);
    if mvcc_commit_timestamp_manager_a.physical_time_ms != mvcc_commit_timestamp_manager_b.physical_time_ms {
        println("FAIL mvcc_commit_timestamp_manager");
        process_exit(1);
    }
    println("prod_ap_mvcc_commit_timestamp_manager: PASSED");
}
