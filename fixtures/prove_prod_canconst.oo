// # Product sec stackcanary const prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sec/sys/stackcanary_const.oo.
//
// Setup: Product std/sec/sys/stackcanary_const.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. disabled 0 all 3. magic cookie 3735928559.
//   9. Exit 1 before PASSED on a miss.

import "std/sec/sys/stackcanary_const.oo";

pub fn main() {
    let a: Int = canary_level_disabled();
    if a != 0 {
        println("FAIL can dis");
        process_exit(1);
    }
    let b: Int = canary_level_all();
    if b != 3 {
        println("FAIL can all");
        process_exit(1);
    }
    let c: Int = canary_default_magic_cookie();
    if c != 3735928559 {
        println("FAIL can cookie");
        process_exit(1);
    }
    println("prod_canconst: PASSED");
}
