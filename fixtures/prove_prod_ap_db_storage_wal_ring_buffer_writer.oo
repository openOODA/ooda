// # Product prove std/db_storage/wal_ring_buffer_writer.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/wal_ring_buffer_writer.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/wal_ring_buffer_writer.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_wal_ring_buffer_writer: PASSED only if they match.

import "std/db_storage/wal_ring_buffer_writer.oo";

pub fn main() {
    let wal_ring_buffer_writer_a = wal_ring_buffer_new(0);
    let wal_ring_buffer_writer_b = wal_ring_buffer_new(0);
    if wal_ring_buffer_writer_a.capacity_slots != wal_ring_buffer_writer_b.capacity_slots {
        println("FAIL wal_ring_buffer_writer");
        process_exit(1);
    }
    println("prod_ap_wal_ring_buffer_writer: PASSED");
}
