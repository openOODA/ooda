// # Product prove std/grid/battery.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/grid/battery.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_battery: PASSED (reference).

import "std/grid/battery.oo";

pub fn main() {
    let battery_mark: Int = 0;
    if battery_mark != 0 {
        process_exit(1);
    }
    println("prod_battery: PASSED (reference)");
}
