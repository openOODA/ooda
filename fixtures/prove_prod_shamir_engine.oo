import "std/sec/crypto/shamir_engine.oo";

pub fn main() {
    let shamir_engine_mark: Int = 0;
    if shamir_engine_mark != 0 { process_exit(1); }
    let a: ShamirConfig = shamir_config_default();
    let b: ShamirConfig = shamir_config_default();
    if a != b { process_exit(1); }
    println("prod_shamir_engine: PASSED");
}
