// MAX_CYCLES: 50
// M126: shallow recursion under shared budget
fn burn(n: Int) -> Int {
    if n <= 0 {
        return 0;
    }
    return burn(n - 1);
}

pub fn main() {
    let x = burn(3);
    println(x);
}
