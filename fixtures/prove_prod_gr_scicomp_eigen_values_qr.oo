// # Product prove std/scicomp/eigen_values_qr.oo
//
// Logline: Fail-closed emit+gcc+run asserts math_sqrt is a real value.
//
// Setup: Isolated emit-c. Imports std/scicomp/eigen_values_qr.oo. Avoids String != and Result unwrap.
//
// Beats:
//   1. Call math_sqrt twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_gr_eigen_values_qr: PASSED only if they match.

import "std/scicomp/eigen_values_qr.oo";

pub fn main() {
    let g0_a = math_sqrt(0.0);
    let g0_b = math_sqrt(0.0);
    if g0_a != g0_b {
        println("FAIL math_sqrt");
        process_exit(1);
    }
    println("prod_gr_eigen_values_qr: PASSED");
}
