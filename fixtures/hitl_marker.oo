// # HITL residual marker
//
// Logline: Name the HITL pause surface without running an interactive harness.
//
// Beats:
//   1. Keep the HITL pause comment the product scans.
//   2. Note this is named surface only, not agent pause/resume.
//   3. Print 0 from main.

// M24 marker fixture — HITL residual at alpha (see bootstrap/HITL.oot)
// HITL: pause
// Product recognizes this comment form as the named HITL surface only.
// Not enforced: no interactive HITL harness; not agent pause/resume product.
pub fn main() {
    println(0);
}
