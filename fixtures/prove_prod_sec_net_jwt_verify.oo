// # Product prove std/sec/net/jwt_verify.oo
//
// Logline: Fail-closed emit+check asserts __dummy_std_src_ooda_sec_net_jwt_verify_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/net/jwt_verify.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_net_jwt_verify_oo twice with no args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_jwt_verify: PASSED only if they match.

import "std/sec/net/jwt_verify.oo";

pub fn main() {
    let jwt_verify_mark: Int = 0;
    if jwt_verify_mark != 0 { process_exit(1); }
    let a: Int = __dummy_std_src_ooda_sec_net_jwt_verify_oo();
    let b: Int = __dummy_std_src_ooda_sec_net_jwt_verify_oo();
    if a != b { process_exit(1); }
    println("prod_jwt_verify: PASSED");
}
