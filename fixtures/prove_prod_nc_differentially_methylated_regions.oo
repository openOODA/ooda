// # Product prove std/genomics_advanced/differentially_methylated_regions.oo
//
// Logline: Fail-closed emit+gcc+run asserts differentially_methylated_regions_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/genomics_advanced/differentially_methylated_regions.oo.
//
// Beats:
//   1. Call differentially_methylated_regions_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_differentially_methylated_regions: PASSED only if it matches.

import "std/genomics_advanced/differentially_methylated_regions.oo";

pub fn main() {
    let a: Int = differentially_methylated_regions_probe();
    if a != 0 {
        println("FAIL differentially_methylated_regions_probe");
        process_exit(1);
    }
    println("prod_nc_differentially_methylated_regions: PASSED");
}
