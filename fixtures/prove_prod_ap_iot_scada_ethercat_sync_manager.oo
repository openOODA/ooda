// # Product prove std/iot_scada/ethercat_sync_manager.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/iot_scada/ethercat_sync_manager.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/iot_scada/ethercat_sync_manager.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_ethercat_sync_manager: PASSED only if they match.

import "std/iot_scada/ethercat_sync_manager.oo";

pub fn main() {
    let ethercat_sync_manager_a = ethercat_sm_mailbox_out(0, 0);
    let ethercat_sync_manager_b = ethercat_sm_mailbox_out(0, 0);
    if ethercat_sync_manager_a.channel_index != ethercat_sync_manager_b.channel_index {
        println("FAIL ethercat_sync_manager");
        process_exit(1);
    }
    println("prod_ap_ethercat_sync_manager: PASSED");
}
