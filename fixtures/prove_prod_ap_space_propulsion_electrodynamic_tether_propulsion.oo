// # Product prove std/space_propulsion/electrodynamic_tether_propulsion.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/electrodynamic_tether_propulsion.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/electrodynamic_tether_propulsion.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_electrodynamic_tether_propulsion: PASSED only if they match.

import "std/space_propulsion/electrodynamic_tether_propulsion.oo";

pub fn main() {
    let electrodynamic_tether_propulsion_a = edt_spec_default_leo_deorbit();
    let electrodynamic_tether_propulsion_b = edt_spec_default_leo_deorbit();
    if electrodynamic_tether_propulsion_a.tether_length_km != electrodynamic_tether_propulsion_b.tether_length_km {
        println("FAIL electrodynamic_tether_propulsion");
        process_exit(1);
    }
    println("prod_ap_electrodynamic_tether_propulsion: PASSED");
}
