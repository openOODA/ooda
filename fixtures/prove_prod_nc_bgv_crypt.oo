// # Product prove std/sec/crypto/bgv_crypt.oo
//
// Logline: Fail-closed emit+gcc+run asserts bgv_crypt_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/sec/crypto/bgv_crypt.oo.
//
// Beats:
//   1. Call bgv_crypt_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_bgv_crypt: PASSED only if it matches.

import "std/sec/crypto/bgv_crypt.oo";

pub fn main() {
    let a: Int = bgv_crypt_probe();
    if a != 0 {
        println("FAIL bgv_crypt_probe");
        process_exit(1);
    }
    println("prod_nc_bgv_crypt: PASSED");
}
