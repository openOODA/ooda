// # Product prove std/graph_topology/mst_prim.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/graph_topology/mst_prim.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_mst_prim: PASSED (reference).

import "std/graph_topology/mst_prim.oo";

pub fn main() {
    let mst_prim_mark: Int = 0;
    if mst_prim_mark != 0 {
        process_exit(1);
    }
    println("prod_mst_prim: PASSED (reference)");
}
