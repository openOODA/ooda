// # Product prove std/crispr_cas/on_target_deepspcas9_scoring.oo
//
// Logline: Fail-closed emit+gcc+run asserts on_target_deepspcas9_scoring_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/on_target_deepspcas9_scoring.oo.
//
// Beats:
//   1. Call on_target_deepspcas9_scoring_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_on_target_deepspcas9_scoring: PASSED only if it matches.

import "std/crispr_cas/on_target_deepspcas9_scoring.oo";

pub fn main() {
    let a: Int = on_target_deepspcas9_scoring_probe();
    if a != 0 {
        println("FAIL on_target_deepspcas9_scoring_probe");
        process_exit(1);
    }
    println("prod_nc_on_target_deepspcas9_scoring: PASSED");
}
