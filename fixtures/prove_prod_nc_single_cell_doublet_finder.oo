// # Product prove std/genomics_advanced/single_cell_doublet_finder.oo
//
// Logline: Fail-closed emit+gcc+run asserts single_cell_doublet_finder_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/genomics_advanced/single_cell_doublet_finder.oo.
//
// Beats:
//   1. Call single_cell_doublet_finder_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_single_cell_doublet_finder: PASSED only if it matches.

import "std/genomics_advanced/single_cell_doublet_finder.oo";

pub fn main() {
    let a: Int = single_cell_doublet_finder_probe();
    if a != 0 {
        println("FAIL single_cell_doublet_finder_probe");
        process_exit(1);
    }
    println("prod_nc_single_cell_doublet_finder: PASSED");
}
