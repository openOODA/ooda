// # Product prove std/grid/wind_farm.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/grid/wind_farm.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_wind_farm: PASSED (reference).

import "std/grid/wind_farm.oo";

pub fn main() {
    let wind_farm_mark: Int = 0;
    if wind_farm_mark != 0 {
        process_exit(1);
    }
    println("prod_wind_farm: PASSED (reference)");
}
