// # Product prove std/bft_consensus/mempool_fee_priority_queue.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/bft_consensus/mempool_fee_priority_queue.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/bft_consensus/mempool_fee_priority_queue.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_mempool_fee_priority_queue: PASSED only if they match.

import "std/bft_consensus/mempool_fee_priority_queue.oo";

pub fn main() {
    let mempool_fee_priority_queue_a = mempool_queue_new(0, 0);
    let mempool_fee_priority_queue_b = mempool_queue_new(0, 0);
    if mempool_fee_priority_queue_a.total_txs != mempool_fee_priority_queue_b.total_txs {
        println("FAIL mempool_fee_priority_queue");
        process_exit(1);
    }
    println("prod_ap_mempool_fee_priority_queue: PASSED");
}
