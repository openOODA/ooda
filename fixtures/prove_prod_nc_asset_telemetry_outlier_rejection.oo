// # Product prove std/digital_twin/asset_telemetry_outlier_rejection.oo
//
// Logline: Fail-closed emit+gcc+run asserts asset_telemetry_outlier_rejection_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/digital_twin/asset_telemetry_outlier_rejection.oo.
//
// Beats:
//   1. Call asset_telemetry_outlier_rejection_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_asset_telemetry_outlier_rejection: PASSED only if it matches.

import "std/digital_twin/asset_telemetry_outlier_rejection.oo";

pub fn main() {
    let a: Int = asset_telemetry_outlier_rejection_probe();
    if a != 0 {
        println("FAIL asset_telemetry_outlier_rejection_probe");
        process_exit(1);
    }
    println("prod_nc_asset_telemetry_outlier_rejection: PASSED");
}
