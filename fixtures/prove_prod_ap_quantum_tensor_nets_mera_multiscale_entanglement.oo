// # Product prove std/quantum_tensor_nets/mera_multiscale_entanglement.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/mera_multiscale_entanglement.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/mera_multiscale_entanglement.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_mera_multiscale_entanglement: PASSED only if they match.

import "std/quantum_tensor_nets/mera_multiscale_entanglement.oo";

pub fn main() {
    let mera_multiscale_entanglement_a = mera_disentangler_new(0, 0, 0);
    let mera_multiscale_entanglement_b = mera_disentangler_new(0, 0, 0);
    if mera_multiscale_entanglement_a.layer_index != mera_multiscale_entanglement_b.layer_index {
        println("FAIL mera_multiscale_entanglement");
        process_exit(1);
    }
    println("prod_ap_mera_multiscale_entanglement: PASSED");
}
