// # emit-x86 zero-libc freelist and block recycling
pub fn main() {
    let p1 = oo_alloc(32);
    oo_write_int(p1, 100);
    oo_free(p1);
    let p2 = oo_alloc(32);
    oo_write_int(p2, 200);
    println(oo_read_int(p2));
}
