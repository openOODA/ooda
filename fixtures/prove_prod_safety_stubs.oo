// # Product prove std/robot/safety_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/robot/safety_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_safety_stubs: PASSED (reference).

import "std/robot/safety_stubs.oo";

pub fn main() {
    let safety_stubs_mark: Int = 0;
    if safety_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_safety_stubs: PASSED (reference)");
}
