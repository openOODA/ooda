// # Product prove std/robotics_ros2/collision_capsule_cylinder.oo
//
// Logline: Fail-closed emit+gcc+run asserts collision_capsule_cylinder_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/robotics_ros2/collision_capsule_cylinder.oo.
//
// Beats:
//   1. Call collision_capsule_cylinder_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_collision_capsule_cylinder: PASSED only if it matches.

import "std/robotics_ros2/collision_capsule_cylinder.oo";

pub fn main() {
    let a: Int = collision_capsule_cylinder_probe();
    if a != 0 {
        println("FAIL collision_capsule_cylinder_probe");
        process_exit(1);
    }
    println("prod_nc_collision_capsule_cylinder: PASSED");
}
