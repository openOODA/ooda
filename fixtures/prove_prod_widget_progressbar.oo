// # Product prove std/tui/widget_progressbar.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/tui/widget_progressbar.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_widget_progressbar: PASSED (reference).

import "std/tui/widget_progressbar.oo";

pub fn main() {
    let widget_progressbar_mark: Int = 0;
    if widget_progressbar_mark != 0 {
        process_exit(1);
    }
    println("prod_widget_progressbar: PASSED (reference)");
}
