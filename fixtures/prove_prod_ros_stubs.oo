// # Product prove std/robot/ros_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/robot/ros_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_ros_stubs: PASSED (reference).

import "std/robot/ros_stubs.oo";

pub fn main() {
    let ros_stubs_mark: Int = 0;
    if ros_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_ros_stubs: PASSED (reference)");
}
