// M-? closure-style return clean: helper returns constant, caller uses result
pub fn safe_closure() -> String {
    return "[public]";
}
// SECRET: api_key
pub fn main() {
    let api_key = "x";
    let y = safe_closure();
    println(y);
}
