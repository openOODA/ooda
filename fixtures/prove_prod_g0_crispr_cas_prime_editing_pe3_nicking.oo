// # Product prove std/crispr_cas/prime_editing_pe3_nicking.oo
//
// Logline: Fail-closed emit+gcc+run asserts pe3_is_optimal_distance is a real value.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/prime_editing_pe3_nicking.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call pe3_is_optimal_distance twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g0_prime_editing_pe3_nicking: PASSED only if they match.

import "std/crispr_cas/prime_editing_pe3_nicking.oo";

pub fn main() {
    let g0_a = pe3_is_optimal_distance(2);
    let g0_b = pe3_is_optimal_distance(2);
    if g0_a != g0_b {
        println("FAIL pe3_is_optimal_distance");
        process_exit(1);
    }
    println("prod_g0_prime_editing_pe3_nicking: PASSED");
}
