// # Product prove std/crispr_cas/crispr_cas14_cas12j_ssdna.oo
//
// Logline: Fail-closed emit+gcc+run asserts cas14_compute_mismatch_pos is deterministic.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/crispr_cas14_cas12j_ssdna.oo.
//
// Beats:
//   1. Call cas14_compute_mismatch_pos twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_crispr_cas14_cas12j_ssdna: PASSED only if they match.

import "std/crispr_cas/crispr_cas14_cas12j_ssdna.oo";

pub fn main() {
    let crispr_cas14_cas12j_ssdna_mark: Int = 0;
    if crispr_cas14_cas12j_ssdna_mark != 0 {
        process_exit(1);
    }
    let a: Int = cas14_compute_mismatch_pos("x", "x");
    let b: Int = cas14_compute_mismatch_pos("x", "x");
    if a != b {
        println("FAIL cas14_compute_mismatch_pos");
        process_exit(1);
    }
    println("prod_crispr_cas14_cas12j_ssdna: PASSED");
}
