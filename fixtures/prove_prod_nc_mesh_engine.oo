// # Product prove std/edge/mesh_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts mesh_engine_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/edge/mesh_engine.oo.
//
// Beats:
//   1. Call mesh_engine_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_mesh_engine: PASSED only if it matches.

import "std/edge/mesh_engine.oo";

pub fn main() {
    let a: Int = mesh_engine_probe();
    if a != 0 {
        println("FAIL mesh_engine_probe");
        process_exit(1);
    }
    println("prod_nc_mesh_engine: PASSED");
}
