// # Product prove std/db_storage/lsm_sstable_reader.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/db_storage/lsm_sstable_reader.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_lsm_sstable_reader: PASSED (reference).

import "std/db_storage/lsm_sstable_reader.oo";

pub fn main() {
    let lsm_sstable_reader_mark: Int = 0;
    if lsm_sstable_reader_mark != 0 {
        process_exit(1);
    }
    println("prod_lsm_sstable_reader: PASSED (reference)");
}
