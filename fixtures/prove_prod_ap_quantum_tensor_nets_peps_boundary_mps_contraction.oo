// # Product prove std/quantum_tensor_nets/peps_boundary_mps_contraction.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/peps_boundary_mps_contraction.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/peps_boundary_mps_contraction.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_peps_boundary_mps_contraction: PASSED only if they match.

import "std/quantum_tensor_nets/peps_boundary_mps_contraction.oo";

pub fn main() {
    let peps_boundary_mps_contraction_a = boundary_mps_config_default(0);
    let peps_boundary_mps_contraction_b = boundary_mps_config_default(0);
    if peps_boundary_mps_contraction_a.boundary_chi_max != peps_boundary_mps_contraction_b.boundary_chi_max {
        println("FAIL peps_boundary_mps_contraction");
        process_exit(1);
    }
    println("prod_ap_peps_boundary_mps_contraction: PASSED");
}
