// M165 path A: verify_human free builtin → Result (env-gated at runtime)
pub fn main() {
    let r: Result[String, String] = verify_human("approve path A?");
    if r.is_ok() {
        println("approved");
    } else {
        println("denied");
    }
}
