// # List weak multi sample
//
// Logline: Return the first of two lists as the weak list multi-arg sample.
//
// Beats:
//   1. Keep FUZZ_DOMAIN list and target two_list 0 4.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// Path A: multi List domain (weak multi)
// FUZZ_DOMAIN: list
// FUZZ_TARGET: two_list 0 4
// FUZZ_ENSURES: two_list result == x
fn two_list(x: List[Int], y: List[Int]) -> List[Int] {
    return x;
}

pub fn main() {
}
