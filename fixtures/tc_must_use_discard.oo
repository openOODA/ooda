// # Discarded user Result call
//
// Logline: A user function that returns Result used as a statement must fail check.
//
// Setup: Single-file probe. boom returns Result[Int, String].
//
// Beats:
//   1. boom returns Result.
//   2. main calls boom as a statement.
//   3. check must ERR unused Result (must-use; do not discard boom).

fn boom() -> Result[Int, String] {
    return Ok(1);
}

pub fn main() {
    boom();
}
