// # Product prove std/robot/motors_ops.oo
//
// Logline: Fail-closed emit+gcc+run asserts math_pi is a real value.
//
// Setup: Isolated emit-c. Imports std/robot/motors_ops.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call math_pi twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_motors_ops: PASSED only if they match.

import "std/robot/motors_ops.oo";

pub fn main() {
    let g2_a = math_pi();
    let g2_b = math_pi();
    if g2_a != g2_b {
        println("FAIL math_pi");
        process_exit(1);
    }
    println("prod_g2_motors_ops: PASSED");
}
