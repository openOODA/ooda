// # Product prove std/sec/net/jwt_header_payload.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/sec/net/jwt_header_payload.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/net/jwt_header_payload.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_jwt_header_payload: PASSED only if they match.

import "std/sec/net/jwt_header_payload.oo";

pub fn main() {
    let jwt_header_payload_a = __dummy_std_src_ooda_sec_net_jwt_header_payload_oo();
    let jwt_header_payload_b = __dummy_std_src_ooda_sec_net_jwt_header_payload_oo();
    if jwt_header_payload_a != jwt_header_payload_b {
        println("FAIL jwt_header_payload");
        process_exit(1);
    }
    println("prod_ap_jwt_header_payload: PASSED");
}
