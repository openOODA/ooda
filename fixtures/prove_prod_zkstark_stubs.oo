import "std/sec/crypto/zkstark_stubs.oo";

pub fn main() {
    let zkstark_stubs_mark: Int = 0;
    if zkstark_stubs_mark != 0 { process_exit(1); }
    let a: Result[Int, String] = stark_eval_merkle_vector_commitment(0, "x");
    let b: Result[Int, String] = stark_eval_merkle_vector_commitment(0, "x");
    if a != b { process_exit(1); }
    println("prod_zkstark_stubs: PASSED");
}
