import "std/sec/crypto/secp256k1_stubs.oo";

pub fn main() {
    let secp256k1_stubs_mark: Int = 0;
    if secp256k1_stubs_mark != 0 { process_exit(1); }
    let a: Result[Secp256k1Point, String] = secp256k1_point_add(0, 0);
    let b: Result[Secp256k1Point, String] = secp256k1_point_add(0, 0);
    if a != b { process_exit(1); }
    println("prod_secp256k1_stubs: PASSED");
}
