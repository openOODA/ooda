// # Product prove std/space_propulsion/helicon_plasma_thruster.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/helicon_plasma_thruster.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/helicon_plasma_thruster.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_helicon_plasma_thruster: PASSED only if they match.

import "std/space_propulsion/helicon_plasma_thruster.oo";

pub fn main() {
    let helicon_plasma_thruster_a = helicon_spec_default_argon();
    let helicon_plasma_thruster_b = helicon_spec_default_argon();
    if helicon_plasma_thruster_a.rf_power_w != helicon_plasma_thruster_b.rf_power_w {
        println("FAIL helicon_plasma_thruster");
        process_exit(1);
    }
    println("prod_ap_helicon_plasma_thruster: PASSED");
}
