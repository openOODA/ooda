// # Product prove std/deep_ai/kv_cache_quant_4bit.oo
//
// Logline: Fail-closed emit+gcc+run asserts kv_cache_quant_4bit_marker is a real value.
//
// Setup: Isolated emit-c. Imports std/deep_ai/kv_cache_quant_4bit.oo. Avoids String != and Result unwrap.
//
// Beats:
//   1. Call kv_cache_quant_4bit_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_gr_kv_cache_quant_4bit: PASSED only if they match.

import "std/deep_ai/kv_cache_quant_4bit.oo";

pub fn main() {
    let g0_a = kv_cache_quant_4bit_marker();
    let g0_b = kv_cache_quant_4bit_marker();
    if g0_a != g0_b {
        println("FAIL kv_cache_quant_4bit_marker");
        process_exit(1);
    }
    println("prod_gr_kv_cache_quant_4bit: PASSED");
}
