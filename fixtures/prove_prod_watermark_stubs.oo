// # Product prove std/sec/watermark_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/sec/watermark_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_watermark_stubs: PASSED (reference).

import "std/sec/watermark_stubs.oo";

pub fn main() {
    let watermark_stubs_mark: Int = 0;
    if watermark_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_watermark_stubs: PASSED (reference)");
}
