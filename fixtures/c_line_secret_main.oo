// # Entry file for SECRET-concat #line
//
// Logline: Import the lib and carry a SECRET: marker so emit-c uses the concat gate.
//
// Setup: Default emit-c (no EMIT_NO_CONCAT). Marker is present; do not print the secret.
//
// Beats:
//   1. Import the lib.
//   2. Include SECRET: so concat src contains the marker.
//   3. Print two().

import "c_line_secret_lib.oo";

// SECRET: token

pub fn main() {
    let n: Int = two();
    println(n);
}
