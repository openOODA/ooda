// # Product prove std/zk_rollup/sparse_merkle_tree_audit.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/zk_rollup/sparse_merkle_tree_audit.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/zk_rollup/sparse_merkle_tree_audit.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_sparse_merkle_tree_audit: PASSED only if they match.

import "std/zk_rollup/sparse_merkle_tree_audit.oo";

pub fn main() {
    let sparse_merkle_tree_audit_a = smt_sibling_new("x", true, 0);
    let sparse_merkle_tree_audit_b = smt_sibling_new("x", true, 0);
    if sparse_merkle_tree_audit_a.sibling_hash != sparse_merkle_tree_audit_b.sibling_hash {
        println("FAIL sparse_merkle_tree_audit");
        process_exit(1);
    }
    println("prod_ap_sparse_merkle_tree_audit: PASSED");
}
