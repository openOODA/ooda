// # Product prove std/zk_rollup/pedersen_hash_curve.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/zk_rollup/pedersen_hash_curve.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/zk_rollup/pedersen_hash_curve.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_pedersen_hash_curve: PASSED only if they match.

import "std/zk_rollup/pedersen_hash_curve.oo";

pub fn main() {
    let pedersen_hash_curve_a = pedersen_point_new(0, 0);
    let pedersen_hash_curve_b = pedersen_point_new(0, 0);
    if pedersen_hash_curve_a.x != pedersen_hash_curve_b.x {
        println("FAIL pedersen_hash_curve");
        process_exit(1);
    }
    println("prod_ap_pedersen_hash_curve: PASSED");
}
