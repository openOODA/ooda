// M25 marker fixture — Cap vs FFI residual at alpha (see bootstrap/CAP_FFI.md)
// FFI: residual
// Named residual surface only (comment form; not parsed / not enforced).
// Not enforced: process-local caps do NOT seal C FFI / dlopen / raw pointers.
pub fn main() {
    println(0);
}
