// # LSP residual marker
//
// Logline: Name the native LSP surface without enforcing it.
//
// Beats:
//   1. Keep the LSP residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/NATIVE_LSP.oot
// LSP: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
