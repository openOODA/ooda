// # Product prove std/nerf_3dgs/nerf_roughness_specular_pbr.oo
//
// Logline: Fail-closed emit+gcc+run asserts nerf_roughness_specular_pbr_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/nerf_3dgs/nerf_roughness_specular_pbr.oo.
//
// Beats:
//   1. Call nerf_roughness_specular_pbr_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_nerf_roughness_specular_pbr: PASSED only if it matches.

import "std/nerf_3dgs/nerf_roughness_specular_pbr.oo";

pub fn main() {
    let a: Int = nerf_roughness_specular_pbr_probe();
    if a != 0 {
        println("FAIL nerf_roughness_specular_pbr_probe");
        process_exit(1);
    }
    println("prod_nc_nerf_roughness_specular_pbr: PASSED");
}
