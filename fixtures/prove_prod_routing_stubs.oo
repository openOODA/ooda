// # Product prove std/fin/routing_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/fin/routing_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_routing_stubs: PASSED (reference).

import "std/fin/routing_stubs.oo";

pub fn main() {
    let routing_stubs_mark: Int = 0;
    if routing_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_routing_stubs: PASSED (reference)");
}
