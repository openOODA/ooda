// # Refuse a bare secret println
//
// Logline: Path A refuses println of a bare tagged IDENT.
//
// Beats:
//   1. Tag x at line start.
//   2. Bind that name to a token literal.
//   3. Print x so emit and check refuse.

// M52 path A — Secret→println bare IDENT refuse (Backend-C emit)
// SECRET: x
pub fn main() {
    let x = "token";
    println(x);
}
