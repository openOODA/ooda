// # Product prove std/space_propulsion/low_thrust_edelbaum_transfer.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/low_thrust_edelbaum_transfer.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/low_thrust_edelbaum_transfer.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_low_thrust_edelbaum_transfer: PASSED only if they match.

import "std/space_propulsion/low_thrust_edelbaum_transfer.oo";

pub fn main() {
    let low_thrust_edelbaum_transfer_a = edelbaum_spec_default_leo_to_geo();
    let low_thrust_edelbaum_transfer_b = edelbaum_spec_default_leo_to_geo();
    if low_thrust_edelbaum_transfer_a.r_initial_m != low_thrust_edelbaum_transfer_b.r_initial_m {
        println("FAIL low_thrust_edelbaum_transfer");
        process_exit(1);
    }
    println("prod_ap_low_thrust_edelbaum_transfer: PASSED");
}
