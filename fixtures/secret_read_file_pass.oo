// M141: non-secret path may read_file (emit path)
// SECRET: token
pub fn main(fs: &FsCap) {
    let token = "/tmp/secret-path";
    let public = "/tmp/ok";
    let r = read_file(fs, public);
    println(1);
}
