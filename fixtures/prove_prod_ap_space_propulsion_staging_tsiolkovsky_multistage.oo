// # Product prove std/space_propulsion/staging_tsiolkovsky_multistage.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/staging_tsiolkovsky_multistage.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/staging_tsiolkovsky_multistage.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_staging_tsiolkovsky_multistage: PASSED only if they match.

import "std/space_propulsion/staging_tsiolkovsky_multistage.oo";

pub fn main() {
    let staging_tsiolkovsky_multistage_a = two_stage_spec_default_orbital();
    let staging_tsiolkovsky_multistage_b = two_stage_spec_default_orbital();
    if staging_tsiolkovsky_multistage_a.payload_mass_kg != staging_tsiolkovsky_multistage_b.payload_mass_kg {
        println("FAIL staging_tsiolkovsky_multistage");
        process_exit(1);
    }
    println("prod_ap_staging_tsiolkovsky_multistage: PASSED");
}
