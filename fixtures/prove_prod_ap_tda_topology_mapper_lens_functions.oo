// # Product prove std/tda_topology/mapper_lens_functions.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/tda_topology/mapper_lens_functions.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/tda_topology/mapper_lens_functions.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_mapper_lens_functions: PASSED only if they match.

import "std/tda_topology/mapper_lens_functions.oo";

pub fn main() {
    let mapper_lens_functions_a = lens_config_coordinate(0);
    let mapper_lens_functions_b = lens_config_coordinate(0);
    if mapper_lens_functions_a.lens_type != mapper_lens_functions_b.lens_type {
        println("FAIL mapper_lens_functions");
        process_exit(1);
    }
    println("prod_ap_mapper_lens_functions: PASSED");
}
