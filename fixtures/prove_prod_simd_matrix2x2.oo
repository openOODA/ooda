// # Product prove std/simd/simd_matrix2x2.oo
//
// Logline: Fail-closed emit+gcc+run asserts simd_mat2_det is deterministic.
//
// Setup: Isolated emit-c. Imports std/simd/simd_matrix2x2.oo.
//
// Beats:
//   1. Call simd_mat2_det twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_simd_matrix2x2: PASSED only if they match.

import "std/simd/simd_matrix2x2.oo";

pub fn main() {
    let obj = simd_mat2_new(0.0, 0.0, 0.0, 0.0);
    let a = simd_mat2_det(obj);
    let b = simd_mat2_det(obj);
    if a != b {
        println("FAIL simd_mat2_det");
        process_exit(1);
    }
    println("prod_simd_matrix2x2: PASSED");
}
