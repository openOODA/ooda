// # Product prove std/physics_advanced/compressible_riemann_roe_solver.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/physics_advanced/compressible_riemann_roe_solver.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/physics_advanced/compressible_riemann_roe_solver.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_compressible_riemann_roe_solver: PASSED only if they match.

import "std/physics_advanced/compressible_riemann_roe_solver.oo";

pub fn main() {
    let compressible_riemann_roe_solver_a = euler_state_new(0.0, 0.0, 0.0, 0.0);
    let compressible_riemann_roe_solver_b = euler_state_new(0.0, 0.0, 0.0, 0.0);
    if compressible_riemann_roe_solver_a.rho != compressible_riemann_roe_solver_b.rho {
        println("FAIL compressible_riemann_roe_solver");
        process_exit(1);
    }
    println("prod_ap_compressible_riemann_roe_solver: PASSED");
}
