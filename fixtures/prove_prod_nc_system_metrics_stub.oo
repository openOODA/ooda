// # Product prove std/os/system_metrics_stub.oo
//
// Logline: Fail-closed emit+gcc+run asserts system_metrics_stub_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/os/system_metrics_stub.oo.
//
// Beats:
//   1. Call system_metrics_stub_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_system_metrics_stub: PASSED only if it matches.

import "std/os/system_metrics_stub.oo";

pub fn main() {
    let a: Int = system_metrics_stub_probe();
    if a != 0 {
        println("FAIL system_metrics_stub_probe");
        process_exit(1);
    }
    println("prod_nc_system_metrics_stub: PASSED");
}
