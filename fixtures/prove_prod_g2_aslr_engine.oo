// # Product prove std/sec/sys/aslr_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts __dummy_std_src_ooda_sec_sys_aslr_engine_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/sys/aslr_engine.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_sys_aslr_engine_oo twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_aslr_engine: PASSED only if they match.

import "std/sec/sys/aslr_engine.oo";

pub fn main() {
    let aslr_engine_mark: Int = 0;
    if aslr_engine_mark != 0 {
        process_exit(1);
    }
    let a: Int = __dummy_std_src_ooda_sec_sys_aslr_engine_oo();
    let b: Int = __dummy_std_src_ooda_sec_sys_aslr_engine_oo();
    if a != b {
        println("FAIL __dummy_std_src_ooda_sec_sys_aslr_engine_oo");
        process_exit(1);
    }
    println("prod_g2_aslr_engine: PASSED");
}
