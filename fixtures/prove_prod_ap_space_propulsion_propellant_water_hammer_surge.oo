// # Product prove std/space_propulsion/propellant_water_hammer_surge.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/propellant_water_hammer_surge.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/propellant_water_hammer_surge.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_propellant_water_hammer_surge: PASSED only if they match.

import "std/space_propulsion/propellant_water_hammer_surge.oo";

pub fn main() {
    let propellant_water_hammer_surge_a = propellant_line_spec_default_lox();
    let propellant_water_hammer_surge_b = propellant_line_spec_default_lox();
    if propellant_water_hammer_surge_a.pipe_diameter_m != propellant_water_hammer_surge_b.pipe_diameter_m {
        println("FAIL propellant_water_hammer_surge");
        process_exit(1);
    }
    println("prod_ap_propellant_water_hammer_surge: PASSED");
}
