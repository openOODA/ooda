// T1 if-taint: secret assigned inside if-branch, then used at println sink
// SECRET: secret
pub fn main() {
    let secret = "token";
    let mut s = "public";
    if true {
        s = secret;
    }
    println(s);
}
