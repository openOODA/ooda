// # Product prove std/iot_scada/safety_interlock_matrix.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/iot_scada/safety_interlock_matrix.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/iot_scada/safety_interlock_matrix.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_safety_interlock_matrix: PASSED only if they match.

import "std/iot_scada/safety_interlock_matrix.oo";

pub fn main() {
    let safety_interlock_matrix_a = sis_vote_1oo2(true, true);
    let safety_interlock_matrix_b = sis_vote_1oo2(true, true);
    if safety_interlock_matrix_a.is_trip != safety_interlock_matrix_b.is_trip {
        println("FAIL safety_interlock_matrix");
        process_exit(1);
    }
    println("prod_ap_safety_interlock_matrix: PASSED");
}
