pub fn try_scan(src: String, i0: Int) -> List[String] {
    let n = chars_len(src);
    if i0 >= n {
        return list_new();
    }
    return list_new();
}
pub fn main() {
    let x = try_scan(" ", 0);
    println(list_len(x));
}
