// # Product prove std/quantum_tensor_nets/spin_heisenberg_model_mpo.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/spin_heisenberg_model_mpo.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/spin_heisenberg_model_mpo.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_spin_heisenberg_model_mpo: PASSED only if they match.

import "std/quantum_tensor_nets/spin_heisenberg_model_mpo.oo";

pub fn main() {
    let spin_heisenberg_model_mpo_a = heisenberg_config_xxx(0, 0.0);
    let spin_heisenberg_model_mpo_b = heisenberg_config_xxx(0, 0.0);
    if spin_heisenberg_model_mpo_a.num_sites != spin_heisenberg_model_mpo_b.num_sites {
        println("FAIL spin_heisenberg_model_mpo");
        process_exit(1);
    }
    println("prod_ap_spin_heisenberg_model_mpo: PASSED");
}
