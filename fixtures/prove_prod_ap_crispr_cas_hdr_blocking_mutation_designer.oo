// # Product prove std/crispr_cas/hdr_blocking_mutation_designer.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/crispr_cas/hdr_blocking_mutation_designer.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/hdr_blocking_mutation_designer.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_hdr_blocking_mutation_designer: PASSED only if they match.

import "std/crispr_cas/hdr_blocking_mutation_designer.oo";

pub fn main() {
    let hdr_blocking_mutation_designer_a = blocking_design_pam_mutation("x");
    let hdr_blocking_mutation_designer_b = blocking_design_pam_mutation("x");
    if hdr_blocking_mutation_designer_a.original_codon != hdr_blocking_mutation_designer_b.original_codon {
        println("FAIL hdr_blocking_mutation_designer");
        process_exit(1);
    }
    println("prod_ap_hdr_blocking_mutation_designer: PASSED");
}
