// # Product prove std/quantum_open_systems/hierarchical_equations_of_motion.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_open_systems/hierarchical_equations_of_motion.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_open_systems/hierarchical_equations_of_motion.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_hierarchical_equations_of_motion: PASSED only if they match.

import "std/quantum_open_systems/hierarchical_equations_of_motion.oo";

pub fn main() {
    let hierarchical_equations_of_motion_a = heom_c_new(0.0, 0.0);
    let hierarchical_equations_of_motion_b = heom_c_new(0.0, 0.0);
    if hierarchical_equations_of_motion_a.real != hierarchical_equations_of_motion_b.real {
        println("FAIL hierarchical_equations_of_motion");
        process_exit(1);
    }
    println("prod_ap_hierarchical_equations_of_motion: PASSED");
}
