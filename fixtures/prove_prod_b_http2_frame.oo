// # Product prove std/net/http2_frame.oo
//
// Logline: Fail-closed emit+gcc+run asserts http2_frame_type_name is deterministic.
//
// Setup: Isolated emit-c. Imports std/net/http2_frame.oo.
//
// Beats:
//   1. Call http2_frame_type_name twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_http2_frame: PASSED only if they match.

import "std/net/http2_frame.oo";

pub fn main() {
    let a = http2_frame_type_name(0);
    let b = http2_frame_type_name(0);
    if a != b {
        println("FAIL http2_frame_type_name");
        process_exit(1);
    }
    println("prod_http2_frame: PASSED");
}
