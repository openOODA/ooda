import "std/sec/crypto/hsm.oo";

pub fn main() {
    let hsm_mark: Int = 0;
    if hsm_mark != 0 { process_exit(1); }
    let a: Int = shim_marker_checksum();
    let b: Int = shim_marker_checksum();
    if a != b { process_exit(1); }
    println("prod_hsm: PASSED");
}
