// Fail fixture: assert_eq deliberately wrong — ooda test must exit non-zero.
pub fn add(a: Int, b: Int) -> Int {
    return a + b;
}

verify add {
    assert_eq!(add(2, 3), 6);
}

pub fn main() {
    let x = add(1, 1);
    println(x);
}
