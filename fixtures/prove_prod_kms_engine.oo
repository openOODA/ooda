import "std/sec/crypto/kms_engine.oo";

pub fn main() {
    let kms_engine_mark: Int = 0;
    if kms_engine_mark != 0 { process_exit(1); }
    let a: KmsEngine = kms_engine_init("x");
    let b: KmsEngine = kms_engine_init("x");
    if a != b { process_exit(1); }
    println("prod_kms_engine: PASSED");
}
