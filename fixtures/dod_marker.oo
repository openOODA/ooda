// # DOD residual marker
//
// Logline: Name the data-oriented layout surface without enforcing it.
//
// Setup: See bootstrap/DOD_LAYOUT.oot. Named only, not enforced.
//
// Beats:
//   1. Keep the DOD residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/DOD_LAYOUT.oot
// DOD: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
