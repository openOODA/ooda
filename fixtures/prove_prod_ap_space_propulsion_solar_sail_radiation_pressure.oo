// # Product prove std/space_propulsion/solar_sail_radiation_pressure.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/solar_sail_radiation_pressure.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/solar_sail_radiation_pressure.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_solar_sail_radiation_pressure: PASSED only if they match.

import "std/space_propulsion/solar_sail_radiation_pressure.oo";

pub fn main() {
    let solar_sail_radiation_pressure_a = solar_sail_spec_default();
    let solar_sail_radiation_pressure_b = solar_sail_spec_default();
    if solar_sail_radiation_pressure_a.sail_area_m2 != solar_sail_radiation_pressure_b.sail_area_m2 {
        println("FAIL solar_sail_radiation_pressure");
        process_exit(1);
    }
    println("prod_ap_solar_sail_radiation_pressure: PASSED");
}
