// # Product prove std/grid/loadflow_types.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/grid/loadflow_types.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/grid/loadflow_types.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_loadflow_types: PASSED only if they match.

import "std/grid/loadflow_types.oo";

pub fn main() {
    let loadflow_types_a = complex_new(0, 0);
    let loadflow_types_b = complex_new(0, 0);
    if loadflow_types_a.re != loadflow_types_b.re {
        println("FAIL loadflow_types");
        process_exit(1);
    }
    println("prod_ap_loadflow_types: PASSED");
}
