import "std/sec/sys/objcap_engine.oo";

pub fn main() {
    let objcap_engine_mark: Int = 0;
    if objcap_engine_mark != 0 { process_exit(1); }
    let a: Result[Bool, String] = objcap_registry_revoke(0, "x", "x", "x", 0);
    let b: Result[Bool, String] = objcap_registry_revoke(0, "x", "x", "x", 0);
    if a != b { process_exit(1); }
    println("prod_objcap_engine: PASSED");
}
