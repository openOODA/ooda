// # Product prove std/pbr_raytracing/bvh_traversal_stack.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/pbr_raytracing/bvh_traversal_stack.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_bvh_traversal_stack: PASSED (reference).

import "std/pbr_raytracing/bvh_traversal_stack.oo";

pub fn main() {
    let bvh_traversal_stack_mark: Int = 0;
    if bvh_traversal_stack_mark != 0 {
        process_exit(1);
    }
    println("prod_bvh_traversal_stack: PASSED (reference)");
}
