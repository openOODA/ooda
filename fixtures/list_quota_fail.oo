// M114: List operations hit quota
pub fn main() {
    let mut lst: List[String] = list_new();
    let mut i = 0;
    while i < 5000000 {
        lst = list_push(lst, "a");
        i = i + 1;
    }
}
