// # Product prove std/db_columnar/columnar_rolling_window.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_columnar/columnar_rolling_window.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_columnar/columnar_rolling_window.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_columnar_rolling_window: PASSED only if they match.

import "std/db_columnar/columnar_rolling_window.oo";

pub fn main() {
    let columnar_rolling_window_a = window_frame_config_sliding(0);
    let columnar_rolling_window_b = window_frame_config_sliding(0);
    if columnar_rolling_window_a.window_size != columnar_rolling_window_b.window_size {
        println("FAIL columnar_rolling_window");
        process_exit(1);
    }
    println("prod_ap_columnar_rolling_window: PASSED");
}
