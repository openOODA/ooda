// # Call arity mismatch
//
// Logline: Check refuses a one-arg function called with zero arguments.
//
// Setup: Single-file probe. Not println. Line of the call stays under 50.
//
// Beats:
//   1. Declare fn f with one Int param.
//   2. Call f with no args.
//   3. check must ERR type argument(s) but got.

fn f(x: Int) {
}

pub fn main() {
    f();
}
