// # Product prove std/tui/widget_input.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/tui/widget_input.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_widget_input: PASSED (reference).

import "std/tui/widget_input.oo";

pub fn main() {
    let widget_input_mark: Int = 0;
    if widget_input_mark != 0 {
        process_exit(1);
    }
    println("prod_widget_input: PASSED (reference)");
}
