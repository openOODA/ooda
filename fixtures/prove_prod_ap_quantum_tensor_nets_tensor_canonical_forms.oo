// # Product prove std/quantum_tensor_nets/tensor_canonical_forms.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/tensor_canonical_forms.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/tensor_canonical_forms.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_tensor_canonical_forms: PASSED only if they match.

import "std/quantum_tensor_nets/tensor_canonical_forms.oo";

pub fn main() {
    let tensor_canonical_forms_a = canonical_gauge_new(0);
    let tensor_canonical_forms_b = canonical_gauge_new(0);
    if tensor_canonical_forms_a.site_index != tensor_canonical_forms_b.site_index {
        println("FAIL tensor_canonical_forms");
        process_exit(1);
    }
    println("prod_ap_tensor_canonical_forms: PASSED");
}
