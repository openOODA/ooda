// M-? method-call clean: chained call returns clean value
pub fn safe_make() -> String {
    return "public";
}
// SECRET: api_key
pub fn main() {
    let api_key = "x";
    let y = safe_make().len();
    println(y);
}
