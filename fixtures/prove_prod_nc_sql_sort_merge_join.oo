// # Product prove std/db_storage/sql_sort_merge_join.oo
//
// Logline: Fail-closed emit+gcc+run asserts sql_sort_merge_join_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/db_storage/sql_sort_merge_join.oo.
//
// Beats:
//   1. Call sql_sort_merge_join_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_sql_sort_merge_join: PASSED only if it matches.

import "std/db_storage/sql_sort_merge_join.oo";

pub fn main() {
    let a: Int = sql_sort_merge_join_probe();
    if a != 0 {
        println("FAIL sql_sort_merge_join_probe");
        process_exit(1);
    }
    println("prod_nc_sql_sort_merge_join: PASSED");
}
