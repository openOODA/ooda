// # Product prove std/db_storage/sql_window_function_executor.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/sql_window_function_executor.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/sql_window_function_executor.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_sql_window_function_executor: PASSED only if they match.

import "std/db_storage/sql_window_function_executor.oo";

pub fn main() {
    let sql_window_function_executor_a = window_execution_result_new();
    let sql_window_function_executor_b = window_execution_result_new();
    if sql_window_function_executor_a.total_processed != sql_window_function_executor_b.total_processed {
        println("FAIL sql_window_function_executor");
        process_exit(1);
    }
    println("prod_ap_sql_window_function_executor: PASSED");
}
