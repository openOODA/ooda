// # Product prove std/synthetic_biology/synthetic_degron_tagging.oo
//
// Logline: Fail-closed emit+gcc+run asserts synthetic_degron_tagging_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/synthetic_biology/synthetic_degron_tagging.oo.
//
// Beats:
//   1. Call synthetic_degron_tagging_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_synthetic_degron_tagging: PASSED only if it matches.

import "std/synthetic_biology/synthetic_degron_tagging.oo";

pub fn main() {
    let a: Int = synthetic_degron_tagging_probe();
    if a != 0 {
        println("FAIL synthetic_degron_tagging_probe");
        process_exit(1);
    }
    println("prod_nc_synthetic_degron_tagging: PASSED");
}
