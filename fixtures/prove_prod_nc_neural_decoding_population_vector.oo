// # Product prove std/neuro_bci/neural_decoding_population_vector.oo
//
// Logline: Fail-closed emit+gcc+run asserts neural_decoding_population_vector_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/neuro_bci/neural_decoding_population_vector.oo.
//
// Beats:
//   1. Call neural_decoding_population_vector_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_neural_decoding_population_vector: PASSED only if it matches.

import "std/neuro_bci/neural_decoding_population_vector.oo";

pub fn main() {
    let a: Int = neural_decoding_population_vector_probe();
    if a != 0 {
        println("FAIL neural_decoding_population_vector_probe");
        process_exit(1);
    }
    println("prod_nc_neural_decoding_population_vector: PASSED");
}
