// # Product prove std/fin/yield_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/fin/yield_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_yield_stubs: PASSED (reference).

import "std/fin/yield_stubs.oo";

pub fn main() {
    let yield_stubs_mark: Int = 0;
    if yield_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_yield_stubs: PASSED (reference)");
}
