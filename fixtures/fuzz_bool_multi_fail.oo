// # Bool arity-2 fuzz fail
//
// Logline: Return y while ensuring result == x so the arity-2 bool rail fails.
//
// Beats:
//   1. Keep FUZZ_DOMAIN bool and target wrong.
//   2. Ensure result == x but return y.
//   3. Leave main empty; the fuzzer drives the target.

// M56 pure Bool arity-2 multi-arg fuzz fail-rail (ensures broken)
// FUZZ_DOMAIN: bool
// FUZZ_TARGET: wrong
// FUZZ_ENSURES: wrong result == x
fn wrong(x: Bool, y: Bool) -> Bool {
    return y;
}

pub fn main() {
}
