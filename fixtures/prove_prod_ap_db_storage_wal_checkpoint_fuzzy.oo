// # Product prove std/db_storage/wal_checkpoint_fuzzy.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_storage/wal_checkpoint_fuzzy.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_storage/wal_checkpoint_fuzzy.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_wal_checkpoint_fuzzy: PASSED only if they match.

import "std/db_storage/wal_checkpoint_fuzzy.oo";

pub fn main() {
    let wal_checkpoint_fuzzy_a = fuzzy_checkpoint_record_new(0, 0);
    let wal_checkpoint_fuzzy_b = fuzzy_checkpoint_record_new(0, 0);
    if wal_checkpoint_fuzzy_a.checkpoint_id != wal_checkpoint_fuzzy_b.checkpoint_id {
        println("FAIL wal_checkpoint_fuzzy");
        process_exit(1);
    }
    println("prod_ap_wal_checkpoint_fuzzy: PASSED");
}
