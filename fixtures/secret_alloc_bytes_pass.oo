// # Allow public alloc_bytes size
//
// Logline: A non-secret size may reach alloc_bytes while a tagged name sits unused.
//
// Beats:
//   1. Tag token at line start.
//   2. Pass a public size instead.
//   3. Expect emit to allow the call.

// Secret allow non-secret: alloc_bytes
// SECRET: token
pub fn main(al: &AllocCap) {
    let token = 64;
    let public = 8;
    let r = alloc_bytes(al, public);
    println(1);
}
