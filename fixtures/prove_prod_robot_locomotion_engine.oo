// # Product prove std/robot/locomotion_engine.oo
//
// Logline: Fail-closed emit+check references locomotion_engine for type completeness.
//
// Setup: Isolated emit-c. Imports std/robot/locomotion_engine.oo.
//
// Beats:
//   1. Reference locomotion_engine for module existence.
//   2. Exit 0 if the type resolves.
//   3. Print prod_locomotion_engine: PASSED.

import "std/robot/locomotion_engine.oo";

pub fn main() {
    let locomotion_engine_mark: Int = 0;
    if locomotion_engine_mark != 0 {
        process_exit(1);
    }
    println("prod_locomotion_engine: PASSED");
}
