// M161 libfloor path A: thread_spawn with granted ThreadCap → residual Err
// main injects long long thread = oo_cap_grant_thread();
// Honesty: no OS pthread — Result err "thread_spawn residual: path A seal only"
pub fn main(thread: &ThreadCap) {
    let r: Result[String, String] = thread_spawn(thread, "worker");
    if r.is_ok() {
        println("unexpected-ok");
    } else {
        println("thread-spawn-residual-ok");
    }
}
