// # Product prove std/genomics_advanced/genomic_interval_tree.oo
//
// Logline: Fail-closed emit+gcc+run asserts genomic_interval_tree_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/genomics_advanced/genomic_interval_tree.oo.
//
// Beats:
//   1. Call genomic_interval_tree_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_genomic_interval_tree: PASSED only if it matches.

import "std/genomics_advanced/genomic_interval_tree.oo";

pub fn main() {
    let a: Int = genomic_interval_tree_probe();
    if a != 0 {
        println("FAIL genomic_interval_tree_probe");
        process_exit(1);
    }
    println("prod_nc_genomic_interval_tree: PASSED");
}
