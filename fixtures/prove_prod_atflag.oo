// # Product sync atomic flag prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sync/atomic_flag.oo.
//
// Setup: Product std/sync/atomic_flag.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. New flag is clear.
//   9. Exit 1 before PASSED on a miss.

import "std/sync/atomic_flag.oo";

pub fn main() {
    let f: AtomicFlag = atomic_flag_new();
    if atomic_flag_test(f) {
        println("FAIL atflag raised");
        process_exit(1);
    }
    println("prod_atflag: PASSED");
}
