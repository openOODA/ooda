// # Name the secret taint surface
//
// Logline: This marker shows line-start secret tags and a public println that must stay allowed.
//
// Beats:
//   1. Tag x at line start as the named surface.
//   2. Bind a public y beside it.
//   3. Print y so the product floor still allows a non-secret sink.

// M22/M52 marker fixture — Secret surface (see bootstrap/SECRET_TAINT.oot)
// SECRET: x
// Product: line-start SECRET name tags; Backend-C path A refuses println(x).
// Residual: not interprocedural taint; not NetCap/log sinks; not assign-prop.
pub fn main() {
    let x = "token";
    let y = "public";
    println(y);
}
