// # HITL pause deny
//
// Logline: Carry a HITL pause marker so non-interactive check denies.
//
// Beats:
//   1. Keep the HITL pause comment the checker scans.
//   2. Leave main a println(0).
//   3. Check fail-closes in non-interactive deny mode.

// HITL path A: non-interactive deny at check
// HITL: pause
pub fn main() {
    println(0);
}
