// # Product prove std/os/terminal_raw_mode_stub.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/os/terminal_raw_mode_stub.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/os/terminal_raw_mode_stub.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_terminal_raw_mode_stub: PASSED only if they match.

import "std/os/terminal_raw_mode_stub.oo";

pub fn main() {
    let terminal_raw_mode_stub_a = raw_mode_config_new();
    let terminal_raw_mode_stub_b = raw_mode_config_new();
    if terminal_raw_mode_stub_a.echo_enabled != terminal_raw_mode_stub_b.echo_enabled {
        println("FAIL terminal_raw_mode_stub");
        process_exit(1);
    }
    println("prod_ap_terminal_raw_mode_stub: PASSED");
}
