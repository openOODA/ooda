// # Product prove std/quantum_open_systems/quantum_jump_monte_carlo.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_open_systems/quantum_jump_monte_carlo.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_open_systems/quantum_jump_monte_carlo.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_quantum_jump_monte_carlo: PASSED only if they match.

import "std/quantum_open_systems/quantum_jump_monte_carlo.oo";

pub fn main() {
    let quantum_jump_monte_carlo_a = mcwf_c_new(0.0, 0.0);
    let quantum_jump_monte_carlo_b = mcwf_c_new(0.0, 0.0);
    if quantum_jump_monte_carlo_a.real != quantum_jump_monte_carlo_b.real {
        println("FAIL quantum_jump_monte_carlo");
        process_exit(1);
    }
    println("prod_ap_quantum_jump_monte_carlo: PASSED");
}
