// # Product prove std/crispr_cas/on_target_azimuth_gradient.oo
//
// Logline: Fail-closed emit+gcc+run asserts on_target_azimuth_gradient_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/crispr_cas/on_target_azimuth_gradient.oo.
//
// Beats:
//   1. Call on_target_azimuth_gradient_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_on_target_azimuth_gradient: PASSED only if it matches.

import "std/crispr_cas/on_target_azimuth_gradient.oo";

pub fn main() {
    let a: Int = on_target_azimuth_gradient_probe();
    if a != 0 {
        println("FAIL on_target_azimuth_gradient_probe");
        process_exit(1);
    }
    println("prod_nc_on_target_azimuth_gradient: PASSED");
}
