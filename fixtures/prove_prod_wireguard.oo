// # Product prove std/net/wireguard.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/net/wireguard.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_wireguard: PASSED (reference).

import "std/net/wireguard.oo";

pub fn main() {
    let wireguard_mark: Int = 0;
    if wireguard_mark != 0 {
        process_exit(1);
    }
    println("prod_wireguard: PASSED (reference)");
}
