// # Milestone 4 test unresolved method on value receiver
//
// Logline: Verify unresolved method fails closed with ERR\tc_emit\tunresolved method.
//
// Setup: Point struct from m4_multi_struct_lib.oo.
//
// Beats:
//   1. Call non-existent method bad_method on Point.
//   2. Fail closed during c_emit.

import "m4_multi_struct_lib.oo";

pub fn main() {
    let p = Point { x: 1, y: 2 };
    p.non_existent_method(42);
}
