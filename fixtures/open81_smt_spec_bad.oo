// # SMT const spec refuse
//
// Logline: Header spec 1 == 0 is unsat at check and must name smt.
//
// Setup: OPEN-81. Check fails; do not run.
//
// Beats:
//   1. spec 1 == 0 is a constant false clause.
//   2. Check prints ERR smt unsat spec.
//   3. Process exits non-zero.

pub fn bad() -> Int
    spec 1 == 0
{
    return 1;
}

pub fn main() {
    println(bad());
}
