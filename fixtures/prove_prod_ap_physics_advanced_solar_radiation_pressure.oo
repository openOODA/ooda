// # Product prove std/physics_advanced/solar_radiation_pressure.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/physics_advanced/solar_radiation_pressure.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/physics_advanced/solar_radiation_pressure.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_solar_radiation_pressure: PASSED only if they match.

import "std/physics_advanced/solar_radiation_pressure.oo";

pub fn main() {
    let solar_radiation_pressure_a = srp_cannonball_new(0.0, 0.0, 0.0);
    let solar_radiation_pressure_b = srp_cannonball_new(0.0, 0.0, 0.0);
    if solar_radiation_pressure_a.cross_section_area_m2 != solar_radiation_pressure_b.cross_section_area_m2 {
        println("FAIL solar_radiation_pressure");
        process_exit(1);
    }
    println("prod_ap_solar_radiation_pressure: PASSED");
}
