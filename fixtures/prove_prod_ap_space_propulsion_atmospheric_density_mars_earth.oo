// # Product prove std/space_propulsion/atmospheric_density_mars_earth.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/atmospheric_density_mars_earth.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/atmospheric_density_mars_earth.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_atmospheric_density_mars_earth: PASSED only if they match.

import "std/space_propulsion/atmospheric_density_mars_earth.oo";

pub fn main() {
    let atmospheric_density_mars_earth_a = atmosphere_earth_us76(0.0);
    let atmospheric_density_mars_earth_b = atmosphere_earth_us76(0.0);
    if atmospheric_density_mars_earth_a.altitude_m != atmospheric_density_mars_earth_b.altitude_m {
        println("FAIL atmospheric_density_mars_earth");
        process_exit(1);
    }
    println("prod_ap_atmospheric_density_mars_earth: PASSED");
}
