// # Product prove std/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_su2_symmetric_wigner_tensor: PASSED only if they match.

import "std/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo";

pub fn main() {
    let su2_symmetric_wigner_tensor_a = su2_irrep_new(0);
    let su2_symmetric_wigner_tensor_b = su2_irrep_new(0);
    if su2_symmetric_wigner_tensor_a.two_j != su2_symmetric_wigner_tensor_b.two_j {
        println("FAIL su2_symmetric_wigner_tensor");
        process_exit(1);
    }
    println("prod_ap_su2_symmetric_wigner_tensor: PASSED");
}
