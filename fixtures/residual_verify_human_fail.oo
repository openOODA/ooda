// historical residual fixture name — M165: verify_human is path A product free builtin
// Prefer fixtures/hitl_verify_human.oo for path A rails. This file stays check-OK.
pub fn main() {
    let r: Result[String, String] = verify_human("ok?");
    if r.is_ok() {
        println("ok");
    } else {
        println("deny");
    }
}
