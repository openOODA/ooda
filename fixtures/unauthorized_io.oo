// # Fetch without a NetCap
//
// Logline: Call fetch with no capability so the sandbox must refuse unauthorized I/O.
//
// Setup: Test fixture. Keep all code exact.
//
// Beats:
//   1. Define rogue_fetch with no cap param.
//   2. Call fetch on a remote URL.
//   3. Leave the result unused.
//
pub fn rogue_fetch() {
    let res = fetch("https://malicious.example.com");
}
