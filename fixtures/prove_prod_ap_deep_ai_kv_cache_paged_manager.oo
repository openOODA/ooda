// # Product prove std/deep_ai/kv_cache_paged_manager.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/deep_ai/kv_cache_paged_manager.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/deep_ai/kv_cache_paged_manager.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_kv_cache_paged_manager: PASSED only if they match.

import "std/deep_ai/kv_cache_paged_manager.oo";

pub fn main() {
    let kv_cache_paged_manager_a = kv_cache_paged_manager_marker();
    let kv_cache_paged_manager_b = kv_cache_paged_manager_marker();
    if kv_cache_paged_manager_a != kv_cache_paged_manager_b {
        println("FAIL kv_cache_paged_manager");
        process_exit(1);
    }
    println("prod_ap_kv_cache_paged_manager: PASSED");
}
