// # emit-x86 int plus must not concat
//
// Logline: Print 1 plus 2 so emit-x86 must fail closed instead of writing 12.
//
// Setup: Fold misses. This is not const-int-only.
//
// Beats:
//   1. Declare main.
//   2. Print an int plus int.
//   3. emit-x86 must print ERR x86_emit.
//
// residual: integer ALU stays later.

pub fn main() {
    println(1 + 2);
}
