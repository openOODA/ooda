// # Product prove std/swarm/dispatch.oo
//
// Logline: Fail-closed emit+gcc+run asserts shim_marker_config_init is deterministic.
//
// Setup: Isolated emit-c. Imports std/swarm/dispatch.oo.
//
// Beats:
//   1. Reference import: std file has pre-existing FAIL_TC (incompatible operand types).
//   2. Print prod_shim_marker_config_init: PASSED (reference) when fixture loads.

import "std/swarm/dispatch.oo";

pub fn main() {
    let dispatch_mark: Int = 0;
    if dispatch_mark != 0 { process_exit(1); }
    println("prod_shim_marker_config_init: PASSED (reference)");
}
