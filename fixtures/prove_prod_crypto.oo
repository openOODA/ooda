// # Product prove std/crypto/crypto.oo
//
// Logline: Fail-closed emit+gcc+run asserts sha256 is deterministic.
//
// Setup: Isolated emit-c. Imports std/crypto/crypto.oo.
//
// Beats:
//   1. Call sha256 twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_crypto: PASSED only if they match.

import "std/crypto/crypto.oo";

pub fn main() {
    let a = sha256("x");
    let b = sha256("x");
    if a != b {
        println("FAIL sha256");
        process_exit(1);
    }
    println("prod_crypto: PASSED");
}
