// # Product prove std/db_graph/opencypher_physical_planner.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_graph/opencypher_physical_planner.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_graph/opencypher_physical_planner.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_opencypher_physical_planner: PASSED only if they match.

import "std/db_graph/opencypher_physical_planner.oo";

pub fn main() {
    let opencypher_physical_planner_a = physical_operator_new(0, 0, 0, 0);
    let opencypher_physical_planner_b = physical_operator_new(0, 0, 0, 0);
    if opencypher_physical_planner_a.op_id != opencypher_physical_planner_b.op_id {
        println("FAIL opencypher_physical_planner");
        process_exit(1);
    }
    println("prod_ap_opencypher_physical_planner: PASSED");
}
