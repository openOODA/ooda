// # Product prove std/wasm_gc/wasm_gc_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/wasm_gc/wasm_gc_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_wasm_gc_stubs: PASSED (reference).

import "std/wasm_gc/wasm_gc_stubs.oo";

pub fn main() {
    let wasm_gc_stubs_mark: Int = 0;
    if wasm_gc_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_wasm_gc_stubs: PASSED (reference)");
}
