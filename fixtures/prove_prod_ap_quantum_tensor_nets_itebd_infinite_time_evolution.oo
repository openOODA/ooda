// # Product prove std/quantum_tensor_nets/itebd_infinite_time_evolution.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/quantum_tensor_nets/itebd_infinite_time_evolution.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/quantum_tensor_nets/itebd_infinite_time_evolution.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_itebd_infinite_time_evolution: PASSED only if they match.

import "std/quantum_tensor_nets/itebd_infinite_time_evolution.oo";

pub fn main() {
    let itebd_infinite_time_evolution_a = itebd_config_default(0, 0.0);
    let itebd_infinite_time_evolution_b = itebd_config_default(0, 0.0);
    if itebd_infinite_time_evolution_a.phys_dim != itebd_infinite_time_evolution_b.phys_dim {
        println("FAIL itebd_infinite_time_evolution");
        process_exit(1);
    }
    println("prod_ap_itebd_infinite_time_evolution: PASSED");
}
