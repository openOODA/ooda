// # Product prove std/space_propulsion/arcjet_thruster_dynamics.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/space_propulsion/arcjet_thruster_dynamics.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/space_propulsion/arcjet_thruster_dynamics.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_arcjet_thruster_dynamics: PASSED only if they match.

import "std/space_propulsion/arcjet_thruster_dynamics.oo";

pub fn main() {
    let arcjet_thruster_dynamics_a = arcjet_spec_default_hydrazine();
    let arcjet_thruster_dynamics_b = arcjet_spec_default_hydrazine();
    if arcjet_thruster_dynamics_a.arc_current_a != arcjet_thruster_dynamics_b.arc_current_a {
        println("FAIL arcjet_thruster_dynamics");
        process_exit(1);
    }
    println("prod_ap_arcjet_thruster_dynamics: PASSED");
}
