// # Product prove std/db_columnar/columnar_zstd_block_codec.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/db_columnar/columnar_zstd_block_codec.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/db_columnar/columnar_zstd_block_codec.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_columnar_zstd_block_codec: PASSED only if they match.

import "std/db_columnar/columnar_zstd_block_codec.oo";

pub fn main() {
    let columnar_zstd_block_codec_a = zstd_frame_header_default();
    let columnar_zstd_block_codec_b = zstd_frame_header_default();
    if columnar_zstd_block_codec_a.magic_number != columnar_zstd_block_codec_b.magic_number {
        println("FAIL columnar_zstd_block_codec");
        process_exit(1);
    }
    println("prod_ap_columnar_zstd_block_codec: PASSED");
}
