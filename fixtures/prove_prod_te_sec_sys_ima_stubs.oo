// # Product prove std/sec/sys/ima_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts __dummy_std_src_ooda_sec_sys_ima_stubs_oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/sys/ima_stubs.oo.
//
// Beats:
//   1. Call __dummy_std_src_ooda_sec_sys_ima_stubs_oo twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ima_stubs: PASSED only if they match.

import "std/sec/sys/ima_stubs.oo";

pub fn main() {
    let ima_stubs_mark: Int = 0;
    if ima_stubs_mark != 0 {
        process_exit(1);
    }
    let a: Int = __dummy_std_src_ooda_sec_sys_ima_stubs_oo();
    let b: Int = __dummy_std_src_ooda_sec_sys_ima_stubs_oo();
    if a != b {
        println("FAIL __dummy_std_src_ooda_sec_sys_ima_stubs_oo");
        process_exit(1);
    }
    println("prod_ima_stubs: PASSED");
}
