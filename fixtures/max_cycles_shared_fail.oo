// M58 shared fuel: two whiles share one budget (each alone under N, sum over)
// MAX_CYCLES: 5
pub fn main() {
    let mut a = 0;
    while a < 3 {
        a = a + 1;
    }
    let mut b = 0;
    while b < 3 {
        b = b + 1;
    }
    println(a + b);
}
