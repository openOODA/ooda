// # Product prove std/zk_rollup/sparse_merkle_tree_batch.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/zk_rollup/sparse_merkle_tree_batch.oo field determinism.
//
// Setup: Isolated emit-c. Imports std/zk_rollup/sparse_merkle_tree_batch.oo.
//
// Beats:
//   1. Build two values the same way.
//   2. Exit 1 if a comparable field differs.
//   3. Print prod_ap_sparse_merkle_tree_batch: PASSED only if they match.

import "std/zk_rollup/sparse_merkle_tree_batch.oo";

pub fn main() {
    let sparse_merkle_tree_batch_a = smt_batch_entry_new("x", "x");
    let sparse_merkle_tree_batch_b = smt_batch_entry_new("x", "x");
    if sparse_merkle_tree_batch_a.key_hex != sparse_merkle_tree_batch_b.key_hex {
        println("FAIL sparse_merkle_tree_batch");
        process_exit(1);
    }
    println("prod_ap_sparse_merkle_tree_batch: PASSED");
}
