// M6: match Ok/Err value form (no Result return-type arrow in helpers)
pub fn main() {
    let a = match Ok("ok") {
        Ok(v) => v,
        Err(e) => e,
    };
    println(a);
    let b = match Err("bad") {
        Ok(v) => v,
        Err(e) => e,
    };
    println(b);
}
