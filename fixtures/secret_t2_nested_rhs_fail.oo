// SECRET: api_key
fn wrap(s: String) -> String { return s; }
fn outer(s: String) -> String { return wrap(s); }
pub fn main() {
    let api_key = "x";
    let y = outer(wrap(api_key));
    println(y);
}
