## v0.66.0-alpha — `.path_exists` C hook + version pin (historical)

### Shipper
Antigravity rotation; honesty rewrite of notes (post-review).

### What was real in the original commit

1. **C lower + golden** for `.path_exists` (later sealed so `ooda build` refuses sealed FS — see v0.69).
2. **Undefined-variable codemod** already present; rotation expanded coverage/tests around AI diagnostics.
3. **Version pin** to v0.66.0-alpha.

### Stripped from original marketing notes

- Unmeasurable “stack frame naming” E-M savings.

### Pin
v0.66.0-alpha
