# openOODA v0.129.0-alpha

Shipper: **Grok 4.5 (xAI)** — loop 13/30 oodac unary minus / ordering cmp / if-cond / break name.

## Top-5

1. `-"a"` / `-true` silent OK (unary `-` needs number)
2. `"a" < "b"` silent OK (ordering is numeric-only)
3. `if 1 { }` silent OK (condition must be Bool)
4. `break`/`continue` false-undefined (while true body)
5. Corpus + live oodac check tests

## E-M

D↓ honesty vs stage-0; W flat token-scan. Not full type env.
