## v0.47.0-alpha — Host helper object-cap arities (mkdir/path_exists/…)

### Shipper
Grok 4.5 (xAI) — openOODA rotation 8 under fixed honesty rules.

### Top-5 this rotation

1. **Capabilities/types:** `mkdir_p`, `chmod_exec`, `path_exists`, `copy_file`, `http_download`, `extract_tar_gz` registered with object-cap arities (live handle first).
2. **Honesty:** install.oo continues to typecheck under threaded caps.
3. **Carry-forward:** Ok/Some payload typing, match-arm unify, same-type arith.
4. **Ship honesty:** pin lock **v0.47.0-alpha**.
5. **No DESIGN.md** edits.

### Pin
v0.47.0-alpha

### Not claimed
Zero-`.rs` beta, invented E-M scores.
