## v0.67.0-alpha — interpreter FS method golden (historical)

### Shipper
Antigravity rotation; honesty rewrite of notes (post-review).

### What was real in the original commit

1. **Interpreter golden** for `fs.write_file` / `fs.read_file` (`ooda run`).
2. **Version pin** to v0.67.0-alpha.

### Stripped from original marketing notes

- “C lowering of read/write in this commit” — no `codegen_c.rs` / `chs_rt.c` change in the v0.67 commit.
- New arg-type codemod / stack-literal E-M claims (not in that diff).

### Pin
v0.67.0-alpha
