## v0.31.0-alpha — Honest E-M, lean QA, one pin

### Top-5 this rotation

1. **E-M honesty (Ps = V·(T−D)/W framing):** `ooda em` measures real parse/cap/typecheck µs and source weight. Removed `EmSavings` 82.4% / floor-score theater.
2. **AI diagnostics:** `--json-errors` goldens require measured `timings_us` and ban `em_savings`.
3. **QA weight ↓:** high-value suite collapsed from ~100 clones to 14 lean files (3 math / 5 real sec_pen traps / 3 stress / 3 ai). Portable `OODA_BIN`.
4. **Version pin lock:** Cargo, clap, `CANONICAL_*`, `CARGO_PKG_VERSION` assert, install pins, website → **v0.31.0-alpha**.
5. **README truth:** migrate is real (edition 2026 match codemod); stage-0 still Rust; beta zero-`.rs` remains the goal (`bootstrap/BETA.md`).

### Not claimed
Full self-host / zero `.rs`, LSP, pkg install, AES, PyTorch, full LLVM/WASM product.
