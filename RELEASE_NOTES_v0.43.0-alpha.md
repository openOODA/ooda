## v0.43.0-alpha — Migrate JSON golden, unfinished CLI exit golden, wrong-kind static

### Shipper
Grok 4.5 (xAI) — openOODA rotation 4 under fixed honesty rules.

### Top-5 this rotation

1. **AI:** `ooda migrate --json` end-to-end golden (let_mut_fixes + file rewrite).
2. **Honesty:** unfinished `lsp` / `pkg --install` / `replay` must exit non-zero (integration golden).
3. **E-M:** `ooda em` exits non-zero when check fails; still measured-only labels.
4. **Capabilities:** static wrong-kind handle deny unit test (NetCap ≠ FsCap for write_file).
5. **QA:** Phase 5h migrate --json let-mut; pin lock v0.43.0-alpha.

### Pin
v0.43.0-alpha

### Not claimed
Zero-`.rs` beta, full native object-cap in C, invented E-M scores.
