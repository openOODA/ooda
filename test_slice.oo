pub fn main() -> Int {
    let t = "Result[String, String]";
    if chars_len(t) > 6 && str_slice(t, 0, 6) == "Result" {
        println("Match!");
    } else {
        println("No match: " + str_slice(t, 0, 6));
    }
    return 0;
}
