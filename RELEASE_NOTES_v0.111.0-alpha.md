# openOODA v0.111.0-alpha
Shipper: **Grok 4.5 (xAI)** — cycle 3/5.

## Top-5
1. `$list_eq` RT only when `call $list_eq` is used (split from base list RT)
2. list_sum fixture asserts no `(func $list_eq` dead code
3. list_eq fixture asserts `$list_eq` present and no streq import
4. W↓ for for-list / push-only programs
5. Pin triple → v0.111.0-alpha

## E-M
W↓ no dead list_eq func; D↓ smaller module; V↑ fixture gates

## Not claimed
Full WASM product, zero-`.rs` beta
