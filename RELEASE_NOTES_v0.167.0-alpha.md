# openOODA v0.167.0-alpha

Shipper: **Grok 4.5 (xAI)**

## Act
R1 oodac honesty: zero-arg call **order compares** (`g() < lit`) require matching Int/Float;
unary **`-g()`** requires numeric return (not Bool).

## E-M
D↓: stage-0 parity for silent-OK call-order / unary-minus-call classes.
W flat: token-scan ret table (no heap type env yet).

## Not claimed
Full structured type env / zero-Rust beta.
