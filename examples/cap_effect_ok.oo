// Sealed effectful net I/O: declare &NetCap and pass the live handle (object-cap).
pub fn fetch_url(net: &NetCap, url: String) -> Result[String, String] {
    return fetch(net, url);
}

pub fn main() {
    println(0);
}
