// Library module imported by import_lib.oo (pure .oo userland)
pub fn sum(a: Int, b: Int) -> Int
    requires a >= 0
    requires b >= 0
    ensures result >= 0
{
    return a + b;
}

verify sum {
    assert_eq!(sum(2, 3), 5);
}
