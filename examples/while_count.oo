// while loops + else-if + unary ! (interpreter + LLVM/WAT subset)
pub fn main() {
    let mut i = 0;
    while i < 3 {
        i = i + 1;
    }
    let flag = !(i == 0);
    let y = if i > 5 {
        9
    } else if i > 2 {
        i
    } else {
        0
    };
    println(y);
}
