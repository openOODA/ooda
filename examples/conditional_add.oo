// LLVM IR Conditional Branching Example
pub fn abs_diff(a: Int, b: Int) -> Int {
    if a > b {
        return a - b;
    } else {
        return b - a;
    }
}

pub fn main() {
    let diff = abs_diff(100, 42);
    println(diff);
}
