--- a/oodac/main.oo
+++ b/oodac/main.oo
@@ -200,8 +200,9 @@
     }
     let env_noarc = env_get(env, "PURE_NO_ARC");
     let mut is_noarc = false;
-    if is_ok(env_noarc) {
-        if res_get(env_noarc) == "1" { is_noarc = true; }
+    if env_noarc.is_ok() {
+        let v = match env_noarc { Ok(s) => s, Err(m) => "" };
+        if v == "1" { is_noarc = true; }
     }
     
     let mut is_emit = false;
@@ -208,12 +209,13 @@
     if cmd == "emit-protos" { is_emit = true; }
     if cmd == "emit-bodies" { is_emit = true; }
     if cmd == "emit-preamble" { is_emit = true; }
-    if is_emit {
+    if is_emit {
         let env_raw = env_get(env, "EMIT_NO_CONCAT");
         let mut do_expand = true;
-        if is_ok(env_raw) {
-            if res_get(env_raw) == "1" { do_expand = false; }
+        if env_raw.is_ok() {
+            let vr = match env_raw { Ok(s) => s, Err(m) => "" };
+            if vr == "1" { do_expand = false; }
         }
         let mut src_text = "";
         if do_expand {
@@ -219,8 +221,8 @@
         } else {
             let src_r = read_file(fs, path);
-            if is_ok(src_r) {
-                src_text = res_get(src_r);
+            if src_r.is_ok() {
+                src_text = match src_r { Ok(s) => s, Err(m) => m };
             } else {
                 println("ERR\tload\tunreadable file: " + path);
                 process_exit(1);
@@ -236,7 +238,17 @@
         if cmd == "emit-preamble" { c_mode = "preamble"; }
         if is_noarc { c_mode = c_mode + "_noarc"; }
         
-        c_emit_stream_mode(src_text, c_mode);
+        if cmd == "emit-c" {
+            let mut m_pre = "preamble";
+            let mut m_pro = "protos";
+            let mut m_bod = "bodies";
+            if is_noarc { m_pre = "preamble_noarc"; m_pro = "protos_noarc"; m_bod = "bodies_noarc"; }
+            c_emit_stream_mode(src_text, m_pre);
+            c_emit_stream_mode(src_text, m_pro);
+            c_emit_stream_mode(src_text, m_bod);
+        } else {
+            c_emit_stream_mode(src_text, c_mode);
+        }
         return;
     }
     if cmd == "run" {
--- a/scripts/oodac_pure_build.sh
+++ b/scripts/oodac_pure_build.sh
@@ -100,13 +100,7 @@
   fi
 fi
 
-FN_DEF='^(void|int|long long|OoStr|OoSList|OoIList|OoResS|OoResV) [A-Za-z_].*\) \{'
-# Optional fallback emit host (cold seed) when primary host flakes/SEGVs.
-SEED_FALLBACK="${OODAC_EMIT_FALLBACK:-}"
-if [[ -z "$SEED_FALLBACK" && -x "$ROOT/bootstrap/seed/oodac" ]]; then
-  SEED_FALLBACK="$ROOT/bootstrap/seed/oodac"
-fi
-MCS=()
+
 echo "pure_build: generating c files"
 
 "$OODAC_BIN" emit-c "$MAIN" > "$TMP/all.c"
