# std core vs os philosophy

**Status:** residual honesty (not full DESIGN green). PM **5.4**.  
**Marker:** `STD_SPLIT_RESIDUAL_ALPHA`

## Named / partial surface

- Directional std split philosophy; product std_* smokes exist partially

## Fail-closed residual

Do **not** treat partial product depth as DESIGN-complete for this item.

## What we do **not** claim

- No complete std::core vs std::os product split shipped

## Rails

- Doc marker: `STD_SPLIT_RESIDUAL_ALPHA`
- Smoke: `std_split_residual_smoke.sh`
- Fixture: `fixtures/std_split_marker.oo`
