# Self-hosted ecosystem residual

**Status:** residual honesty (not full DESIGN green). PM **5.1**.  
**Marker:** `PKG_ECOSYSTEM_RESIDUAL_ALPHA`

## Named / partial surface

- Pure compiler+CLI shipped (5.1a); pkg/LSP/registry residual

## Fail-closed residual

Do **not** treat partial product depth as DESIGN-complete for this item.

## What we do **not** claim

- No full pkg manager / registry / complete self-hosted ecosystem shipped

## Rails

- Doc marker: `PKG_ECOSYSTEM_RESIDUAL_ALPHA`
- Smoke: `pkg_ecosystem_residual_smoke.sh`
- Fixture: `fixtures/pkg_ecosystem_marker.oo`
