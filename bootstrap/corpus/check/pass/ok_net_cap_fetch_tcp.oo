// pass (check): legacy &NetCap supersedes soft-granular for fetch + tcp_connect (CAP-G1)
pub fn ok(net: &NetCap) {
    let a = fetch(net, "http://example.invalid");
    let b = tcp_connect(net, "127.0.0.1", 1);
}
pub fn main() {}
