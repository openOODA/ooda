// pass (check): FsCap legacy supersede for both read_file and write_file (CAP-G2)
pub fn ok(fs: &FsCap) {
    let r = read_file(fs, "/tmp/x");
    let w = write_file(fs, "/tmp/x", "hi");
}
pub fn main() {}
