// fail: non-INT range bounds still residual (only INT..INT lowered)
pub fn main() {
    let lo = 0;
    let hi = 3;
    let mut s = 0;
    for i in lo..hi {
        s = s + i;
    }
    println(s);
}
