// fail: range-for is residual on Backend-C — must ERR\tc_emit (not silent skip)
// Work around: while with manual counter (see bootstrap/FOR_MATCH_RESIDUAL.md)
pub fn main() {
    let mut s = 0;
    for i in 0..5 {
        s = s + i;
    }
    println(s);
}
