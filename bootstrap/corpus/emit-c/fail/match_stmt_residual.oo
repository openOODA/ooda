// fail: statement match is residual on Backend-C — must ERR\tc_emit (not silent skip)
// Work around: if r.is_ok() / is_err() + .val (see bootstrap/FOR_MATCH_RESIDUAL.md)
// Note: `let x = match r { ... }` is a separate (partial) let-RHS path — not this residual.
pub fn main() {
    let r: Result[String, String] = Ok("hi");
    match r {
        Ok(v) => println(v),
        Err(e) => println(e),
    }
}
