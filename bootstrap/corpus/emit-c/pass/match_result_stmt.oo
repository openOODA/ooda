// pass: statement match on Result Ok/Err (Backend-C lowered)
pub fn main() {
    let r: Result[String, String] = Ok("hello");
    match r {
        Ok(v) => println(v),
        Err(e) => println(e),
    }
}
