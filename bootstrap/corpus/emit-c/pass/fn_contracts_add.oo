// Pass: simple requires/ensures present; runtime lower + body still emits.
pub fn add(a: Int, b: Int) -> Int
    requires a >= 0
    requires b >= 0
    ensures result >= 0
{
    return a + b;
}

pub fn main() {
    let x = add(20, 22);
    println(x);
}
