// pass: String local from field_at-like call uses oo_str_eq
pub fn tag() -> String {
    return "OK";
}

pub fn main() {
    let s: String = tag();
    if s == "OK" {
        println(1);
    } else {
        println(0);
    }
}
