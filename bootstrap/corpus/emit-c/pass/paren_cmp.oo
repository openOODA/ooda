// pass: (ri + 1) < rn paren grouping
pub fn main() {
    let mut ri = 0;
    let rn = 3;
    while ri < rn {
        if (ri + 1) < rn {
            println(ri);
        }
        ri = ri + 1;
    }
}
