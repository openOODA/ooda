// pass: assign-form match uses arm exprs (H2)
pub fn main() {
    let mut s = "";
    let r: Result[String, String] = Err("boom");
    s = match r {
        Ok(v) => "OK_ARM",
        Err(e) => "ERR_ARM",
    };
    println(s);
}
