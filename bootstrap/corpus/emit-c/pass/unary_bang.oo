// pass: unary ! and unary -
pub fn is_zero(x: Int) -> Bool {
    return x == 0;
}

pub fn main() {
    let x = 0;
    if !is_zero(1) {
        println(1);
    }
    if !is_zero(0) {
        println(0);
    } else {
        println(2);
    }
    let y = -5;
    println(y);
}
