// pass: multi-arg call with binop args (pack-style)
pub fn pack7(a: Int, b: Int, c: Int, d: String, e: String, f: Int, g: Int) -> Int {
    return a + b + c + f + g;
}

pub fn main() {
    let i = 10;
    let col = 3;
    let r = pack7(i + 1, 1, col + 1, "PLUS", "+", 1, 1);
    println(r);
}
