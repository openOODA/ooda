// pass: call args are full exprs — list_get(xs, i+1) not 4-arg
pub fn main() {
    let mut xs: List[String] = list_new();
    xs = list_push(xs, "a");
    xs = list_push(xs, "b");
    let i = 0;
    println(list_get(xs, i + 1));
    println(list_len(xs));
}
