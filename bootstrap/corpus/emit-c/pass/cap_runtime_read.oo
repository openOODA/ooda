// pass: read_file with real main-injected FsCap magic token (runtime + static seal)
pub fn main(fs: &FsCap) {
    let path = "fixtures/hello.oo";
    let r = read_file(fs, path);
    if r.is_ok() {
        println("ok");
    } else {
        println("err");
    }
}
