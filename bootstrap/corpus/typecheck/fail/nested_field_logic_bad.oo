type Inner = struct {
    v: Int,
};
type Outer = struct {
    inner: Inner,
};
fn test_fn(o: Outer) -> Int {
    if o.inner.v && true {
        return 1;
    }
    return 0;
}
fn main() -> Int {
    return 0;
}
