# Multi-arg pure fuzz (path A)

**Status:** Homogeneous multi-arg pure fuzz is **In** (alpha).  
See `FUZZ_DEFER.md` for product floor and residual (mixed-type multi remains fail-closed).

Rails: `scripts/fuzz_multi_arg_smoke.sh`.
