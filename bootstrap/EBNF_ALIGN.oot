# Formal EBNF living alignment

**Status:** residual honesty (not full DESIGN green). PM **D.1**.  
**Marker:** `EBNF_ALIGN_RESIDUAL_ALPHA`

## Named / partial surface

- ooda.ebnf present; living alignment residual

## Fail-closed residual

Do **not** treat partial product depth as DESIGN-complete for this item.

## What we do **not** claim

- No claim that EBNF is fully aligned with product parser

## Rails

- Doc marker: `EBNF_ALIGN_RESIDUAL_ALPHA`
- Smoke: `ebnf_align_residual_smoke.sh`
- Fixture: `fixtures/ebnf_marker.oo`
