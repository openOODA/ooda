# Full specification residual

**Status:** residual honesty (not full DESIGN green). PM **D.2**.  
**Marker:** `SPEC_DEPTH_RESIDUAL_ALPHA`

## Named / partial surface

- Spec docs partial; not full DESIGN implementation

## Fail-closed residual

Do **not** treat partial product depth as DESIGN-complete for this item.

## What we do **not** claim

- No claim SPEC is complete DESIGN implementation

## Rails

- Doc marker: `SPEC_DEPTH_RESIDUAL_ALPHA`
- Smoke: `spec_depth_residual_smoke.sh`
- Fixture: `fixtures/spec_marker.oo`
