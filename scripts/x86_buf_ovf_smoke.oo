// # emit-x86 bump overflow honesty lock
//
// Logline: emit-x86 of fixtures/x86_buf_ovf.oo prints ERR x86_emit and does not emit _start.
//
// Setup: cwd is ooda/. Product oodac/oodac. Concat length is 4097. Cap is 4096.
//
// Beats:
//   1. emit-x86 overflow fixture.
//   2. Require ERR and x86_emit on stdout or stderr.
//   3. Refuse a successful _start .s.

import "std/scenario.oo";
import "std/sh.oo";

fn fail(msg: String) {
    println("FAIL " + msg);
    process_exit(1);
}

fn pick_oodac(p: &ProcessCap, root: String) -> String {
    return root + "/oodac/oodac";
}

pub fn main(p: &ProcessCap, fs: &FsReadCap, fw: &FsWriteCap) {
    let root = match sh_realpath(p, ".") { Ok(s) => s, Err(_) => "." };
    let tmpd = ".ooda-cache/ooda-tmp";
    if process_exec(p, "mkdir -p " + tmpd).is_err() { fail("mkdir"); }
    if scenario(p, fs, "x86 bump overflow honesty").is_err() { fail("scenario"); }
    if expect_clean(p, "uname -m | grep -qx x86_64").is_err() { fail("host is not x86_64"); }
    let oodac = pick_oodac(p, root);
    if expect_quiet(p, "test -x " + oodac).is_err() { fail("no oodac"); }
    let src = root + "/fixtures/x86_buf_ovf.oo";
    if expect_present(fs, src).is_err() { fail("no x86_buf_ovf.oo"); }
    let sfile = tmpd + "/x86_buf_ovf.s";
    let serr = tmpd + "/x86_buf_ovf.err";
    if process_exec(p, oodac + " emit-x86 " + src + " > " + sfile + " 2>" + serr).is_ok() {
        if expect_clean(p, "grep -q _start " + sfile).is_ok() {
            fail("successful .s omitted overflow ERR");
        }
    }
    if expect_clean(p, "grep -qE 'ERR' " + sfile + " " + serr).is_err() { fail("no ERR"); }
    if expect_clean(p, "grep -q x86_emit " + sfile + " " + serr).is_err() { fail("no x86_emit"); }
    if expect_clean(p, "grep -q buf " + sfile + " " + serr).is_err() { fail("no buf"); }
    if expect_clean(p, "! grep -q _start " + sfile).is_err() { fail("silent omit .s"); }
    println("x86_buf_ovf_smoke: PASSED");
}
