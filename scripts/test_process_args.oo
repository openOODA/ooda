import "std/os/process.oo";

pub fn main(p: &ProcessCap) {
    let p_args: List[String] = process_args(p);
    let n: Int = list_len(p_args);
    let mut i: Int = 0;
    while i < n {
        println("arg[" + i.to_string() + "]=" + list_get(p_args, i));
        i = i + 1;
    }
}
