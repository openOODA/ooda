pub fn main(fs: &FsCap, sys: &SysCap, args: List[String]) {
    let s = "abcdefg";
    let mut i = 0;
    while i < 10000000 {
        let ch = char_at(s, 2);
        i = i + 1;
    }
    println("Done!");
}
