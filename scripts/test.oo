// # Balanced delimiter walk
//
// Logline: Walk a string from j0 looking for matching open and close characters.
//
// Setup: Process capability and test fixtures available.
//
// Beats:
//   1. Import core string helpers.
//   2. Start at j0 and walk while j is in range.
//   3. Return the stop index.

import "../std/core/str.oo";
pub fn bal(s: String, j0: Int, open_c: String, close_c: String) -> Int {
    let mut j = j0;
    let ln = s.len();
    while j < ln {
    }
    return j;
}
