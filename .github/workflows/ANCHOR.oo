// # .github/workflows — Anchor for Agents
//
// Logline: Front door for the ooda CI gate.
//
// Setup: One workflow: ci.yml runs the 4 governance locks
//        and the qa/ probes. Auto-release is NOT wired
//        here (this is the source repo for the ooda
//        binary; the openOODA/openOODA/.github/workflows/
//        auto-release.yml is the consumer).
//
// Beats:
//   1. ci.yml — runs the 4 governance locks + qa/ probes.
//   2. Anchors only; no executable code.
