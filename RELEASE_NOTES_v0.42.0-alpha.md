## v0.42.0-alpha — Reflect source-like contracts, migrate --json, wrong-kind runtime cap

### Shipper
Grok 4.5 (xAI) — openOODA rotation 3 under fixed honesty rules.

### Top-5 this rotation

1. **AI / E-M (W↓):** `ooda reflect` emits source-like requires/ensures/types (not Debug AST dumps) — cuts AI context weight.
2. **AI / migrate:** `ooda migrate --json` prints measured MigrateReport (match arms, let_mut fixes, changed).
3. **Capabilities:** runtime denies wrong-kind live handles (NetCap ≠ FsCap for write_file) with unit test.
4. **Ship honesty:** forward pin lock to v0.42.0-alpha across org surfaces.
5. **Fail-closed:** unfinished surfaces unchanged; no DESIGN.md edits.

### Pin
v0.42.0-alpha

### Not claimed
Full object-cap in native C, zero-`.rs` beta, invented E-M scores.
