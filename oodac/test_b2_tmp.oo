// # Temporary Wasm Add Test
//
// Logline: Test wasm_fn_println_add evaluation.
//
// Setup: Imports token_fmt, wasm_emit_add, and wasm_emit_fold.
//
// Beats:
//   1. Execute test addition and assert expected result.

import "token_fmt.oo";
import "wasm_emit_add.oo";
import "wasm_emit_fold.oo";

pub fn main() {
    let s1: String = "pub fn main() { let x = 2; println(1 + x); }";
    let r1: String = wasm_fn_println_add(s1, "main");
    println("add 1+x=" + r1);
    if r1 == "3" { println("PASS"); } else { println("FAIL"); }
}
