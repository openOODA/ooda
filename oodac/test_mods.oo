import "load_import.oo";
pub fn main(fs: &FsCap, sys: &SysCap, args: List[String]) {
    let le_res: List[String] = load_expand(fs, "oodac/main.oo", "\t", "\t");
    let deps_str = list_get(le_res, 0);
    println(deps_str);
}
