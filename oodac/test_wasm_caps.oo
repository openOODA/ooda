// # Hold Fs and Sys caps and bind one
//
// Logline: A wasm fixture takes &FsCap and &SysCap and only binds a to 1.
//
// Setup: emit-wasm and wasmtime invoke _start on this file; main must keep both caps.
//
// Beats:
//   1. main takes fs and sys so the wasm surface sees those caps.
//   2. Bind a to 1.
//   3. Return with no reads, writes, or prints.

fn main(fs: &FsCap, sys: &SysCap) {
    let a = 1;
}
