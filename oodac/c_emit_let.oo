// package: backend-c — emit floor (see bootstrap/FLOOR.md)
// job: emit let/assign stmts; bind list-kind env
// stage: emit

pub fn c_emit_let_stmt(toks: List[String], pos: Int, n: Int, env: String) -> List[String] {
    let mut p = pos;
    let mut e = env;
    let k = field_at(list_get(toks, p), 0);
    if k == "KW_LET" {
        p = p + 1;
        if p < n && field_at(list_get(toks, p), 0) == "KW_MUT" {
            p = p + 1;
        }
    }
    let name = field_at(list_get(toks, p), 3);
    p = p + 1;
    let mut ann_kind = "";
    let mut ann_ty = "";
    if p < n && field_at(list_get(toks, p), 0) == "COLON" {
        p = p + 1;
        ann_kind = c_peek_list_elem(toks, p, n);
        ann_ty = c_ty_at(toks, p, n);
        p = c_emit_skip_type(toks, p, n);
    }
    if p < n && field_at(list_get(toks, p), 0) == "EQ" {
        p = p + 1;
    }
    // H5: incomplete `let x =` / empty RHS — fail-closed (no `x = ;`)
    if p >= n {
        println("ERR\tc_emit\tincomplete let RHS");
        process_exit(1);
    }
    let rk0 = field_at(list_get(toks, p), 0);
    if rk0 == "SEMI" || rk0 == "RBRACE" || rk0 == "EOF" {
        println("ERR\tc_emit\tincomplete let RHS");
        process_exit(1);
    }
    if p < n && field_at(list_get(toks, p), 0) == "KW_MATCH" {
        return c_emit_match_let(name, toks, p, n, e);
    }
    // if-expression: let y = if c { a } else if d { b } else { e };
    if p < n && field_at(list_get(toks, p), 0) == "KW_IF" {
        let mut lty = "long long";
        if ann_ty != "" && ann_ty != "void" {
            lty = ann_ty;
        }
        println("  " + lty + " " + name + ";");
        let mut ea = c_env_put(e, "__asgn__", name);
        // walk if / else if / else as statements with assign target
        let mut guard = 0;
        while guard < 32 {
            if p >= n {
                break;
            }
            let kk = field_at(list_get(toks, p), 0);
            if kk != "KW_IF" && kk != "KW_ELSE" {
                break;
            }
            let st = c_emit_stmt(toks, p, n, ea);
            p = parse_int(list_get(st, 0));
            ea = list_get(st, 1);
            guard = guard + 1;
        }
        // name is bound as local; no list/str env kind for default long long if-expr
        if p < n && field_at(list_get(toks, p), 0) == "SEMI" {
            p = p + 1;
        }
        return c_pack_pos_env(p, e);
    }
    let mut env_rhs = e;
    if ann_kind == "S" || ann_kind == "I" {
        env_rhs = c_env_put(e, "__ann__", ann_kind);
    }
    let pr = c_emit_expr_c(toks, p, n, env_rhs);
    p = parse_int(list_get(pr, 0));
    let mut rhs = list_get(pr, 1);
    let mut lty = "long long";
    let mut bind_kind = ann_kind;
    // Annotation wins when present (String/Int/Bool/List/Result)
    if ann_ty != "" && ann_ty != "void" {
        lty = ann_ty;
        if ann_ty == "OoSList" {
            bind_kind = "S";
            if rhs == "oo_ilist_new()" {
                rhs = "oo_slist_new()";
            }
        }
        if ann_ty == "OoIList" {
            bind_kind = "I";
        }
    } else if ann_kind == "S" {
        lty = "OoSList";
        if rhs == "oo_ilist_new()" {
            rhs = "oo_slist_new()";
        }
    } else if ann_kind == "I" {
        lty = "OoIList";
    } else {
        // Untyped bind: lit shortcuts first (avoid c_call_result_ty on bare
        // ints/str lits — tree oodac SEGV'd on that path under PURE_NO_ARC).
        let mut crt = "";
        let mut is_num = false;
        if chars_len(rhs) > 0 {
            let c0 = char_at(rhs, 0);
            if c0 == "-" || c0 == "0" || c0 == "1" || c0 == "2" || c0 == "3" || c0 == "4" {
                is_num = true;
            }
            if c0 == "5" || c0 == "6" || c0 == "7" || c0 == "8" || c0 == "9" {
                is_num = true;
            }
        }
        let mut is_str_lit = false;
        if chars_len(rhs) >= 11 {
            if char_at(rhs, 0) == "o" && char_at(rhs, 3) == "s" && char_at(rhs, 4) == "t" {
                if char_at(rhs, 7) == "l" {
                    is_str_lit = true;
                }
            }
        }
        if is_num {
            lty = "long long";
        } else if is_str_lit {
            lty = "OoStr";
        } else {
            let cnm = c_rhs_call_name(rhs);
            // Common CHS helpers without mega-table (avoids SEGV in table walk).
            // Names are lowered (list_new → oo_ilist_new).
            if cnm == "list_new" || cnm == "oo_ilist_new" {
                crt = "OoIList";
            }
            if cnm == "list_push" || cnm == "oo_ilist_push" {
                crt = "OoIList";
            }
            if cnm == "oo_slist_new" || cnm == "oo_slist_push" {
                crt = "OoSList";
            }
            if cnm == "list_get" || cnm == "oo_ilist_get" {
                crt = "long long";
            }
            if cnm == "oo_slist_get" {
                crt = "OoStr";
            }
            if cnm == "list_len" || cnm == "oo_ilist_len" || cnm == "oo_slist_len" {
                crt = "long long";
            }
            if cnm == "chars_len" || cnm == "oo_chars_len" || cnm == "parse_int" {
                crt = "long long";
            }
            if cnm == "char_at" || cnm == "str_slice" || cnm == "int_to_str" {
                crt = "OoStr";
            }
            if cnm == "oo_char_at" || cnm == "oo_str_slice" || cnm == "oo_int_to_str" {
                crt = "OoStr";
            }
            if cnm != "" && crt == "" {
                crt = c_call_result_ty(cnm);
            }
            if crt != "" {
                lty = crt;
                if crt == "OoSList" {
                    bind_kind = "S";
                }
                if crt == "OoIList" {
                    bind_kind = "I";
                }
            } else {
                let ek = c_env_get(e, rhs);
                if ek == "T" {
                    lty = "OoStr";
                } else if ek == "S" {
                    lty = "OoSList";
                    bind_kind = "S";
                } else if ek == "I" {
                    lty = "OoIList";
                    bind_kind = "I";
                } else if chars_len(rhs) >= 9 && char_at(rhs, 0) == "o" {
                    if char_at(rhs, 3) == "i" && char_at(rhs, 4) == "l" { lty = "OoIList"; bind_kind = "I"; }
                    if char_at(rhs, 3) == "s" && char_at(rhs, 4) == "l" {
                        if chars_len(rhs) >= 11 && char_at(rhs, 9) == "g" { lty = "OoStr"; }
                        else { lty = "OoSList"; bind_kind = "S"; }
                    }
                    if char_at(rhs, 3) == "s" && char_at(rhs, 4) == "t" { lty = "OoStr"; }
                    if char_at(rhs, 3) == "c" && char_at(rhs, 4) == "h" { lty = "OoStr"; }
                }
            }
        }
    }
    // Result: Ok/Err → OoResS (R); write_file → OoResV (V) for match/unwrap
    if chars_len(rhs) >= 9 {
        if char_at(rhs, 0) == "(" && char_at(rhs, 2) == "O" {
            lty = "OoResS";
        } else if char_at(rhs, 0) == "(" && char_at(rhs, 2) == "T" {
            lty = "Token";
        }
    }
    if chars_len(rhs) >= 12 {
        if char_at(rhs, 0) == "o" && char_at(rhs, 3) == "w" && char_at(rhs, 4) == "r" {
            // oo_write_file(
            lty = "OoResV";
        }
    }
    if chars_len(rhs) >= 11 {
        if char_at(rhs, 0) == "o" && char_at(rhs, 3) == "r" && char_at(rhs, 4) == "e" {
            // oo_read_file( / oo_env_get roughly — prefer read_file
            if char_at(rhs, 7) == "f" || char_at(rhs, 7) == "g" {
                lty = "OoResS";
            }
        }
    }
    // Result.unwrap() → OoStr (see c_rhs_is_unwrap)
    if c_rhs_is_unwrap(rhs) {
        lty = "OoStr";
    }
    // Single last-wins put: T/R/V beat prior S so String is never stuck as S.
    if lty == "OoStr" {
        e = c_env_put(e, name, "T");
    } else if lty == "OoResS" {
        e = c_env_put(e, name, "R");
    } else if lty == "OoResV" {
        e = c_env_put(e, name, "V");
    } else if bind_kind == "S" || bind_kind == "I" {
        e = c_env_put(e, name, bind_kind);
    }
    println("  " + lty + " " + name + " = " + rhs + ";");
    // Residual M2: let x = y needs retain (double-free risk) — deferred;
    // enabling retain alone regressed self-host heap (see SPRINT notes).
    if p < n && field_at(list_get(toks, p), 0) == "SEMI" {
        p = p + 1;
    }
    return c_pack_pos_env(p, e);
}
