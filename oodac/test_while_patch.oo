// # While Loop Patch Test
//
// Logline: Verifies while loop jump patch offsets.
//
// Setup: Direct jump patch verification.
//
// Beats:
//   1. Assert jump offset calculations.

import "wasm_emit_interp_for.oo";
pub fn main() {
    let src = "pub fn main() { let mut i = 0; while i < 3 { i = i + 1; } let flag = !(i == 0); let y = if i > 5 { 9 } else if i > 2 { i } else { 0 }; println(y); }";
    let out = wi_interp_for_main(src);
    println("out:" + out);
}
