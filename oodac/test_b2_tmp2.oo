// # Temporary Wasm Add Test 2
//
// Logline: Test wasm_fn_println_add second variant.
//
// Setup: Imports wasm_emit_add.
//
// Beats:
//   1. Execute test addition and assert result.

import "wasm_emit_add.oo";
import "wasm_emit_fold.oo";

pub fn main() {
    let s1: String = "pub fn main() { let x = 2; println(1 + x); }";
    let r1: String = wasm_fn_println_add(s1, "main");
    println("add 1+x=" + r1);
    if r1 == "3" { println("PASS"); } else { println("FAIL"); }
}
