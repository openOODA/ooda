// # WebAssembly Lowering Test 5
//
// Logline: Verifies wasm bytecode lowering case 5.
//
// Setup: Direct wasm emission probe.
//
// Beats:
//   1. Lower AST to wasm instructions.

import "wasm_emit_interp.oo";
pub fn main() {
    let src: String = "pub fn main() { let mut s = 0; for i in 0..5 { s = s + i; } println(s); }";
    let out: String = wasm_interp_main(src);
    println("wasm:" + out + ":end");
}
