// # Detect const if (!(INT == INT)) println with no else
//
// Logline: Returns the then STRING when a function body is only if (BANG (INT EQEQ INT)) with one println(STRING) and no else.
//
// Setup: tokenize_lines, field_at, and parse_int exist. fname is the user function name.
//
// Beats:
//   1. Find KW_FN plus IDENT fname and skip to the body brace.
//   2. Accept if LPAREN BANG LPAREN INT EQEQ INT RPAREN RPAREN then one println(STRING) and the function RBRACE.
//   3. Return the then STRING when left != right. IDENT operands miss.
//
// residual: if (!(1 == x)) no-else stays dummy end.

import "token_fmt.oo";
import "tc_parse_int.oo";

fn wpntq_kind(toks: List[String], i: Int) -> String {
    return field_at(list_get(toks, i), 0);
}

fn wpntq_text(toks: List[String], i: Int) -> String {
    return field_at(list_get(toks, i), 3);
}

fn wpntq_one_str_println(toks: List[String], p0: Int, n: Int) -> List[String] {
    let empty: List[String] = list_new();
    let mut p = p0;
    if p + 3 >= n { return empty; }
    if wpntq_kind(toks, p) != "IDENT" || wpntq_text(toks, p) != "println" { return empty; }
    if wpntq_kind(toks, p + 1) != "LPAREN" || wpntq_kind(toks, p + 2) != "STRING" { return empty; }
    if wpntq_kind(toks, p + 3) != "RPAREN" { return empty; }
    let lit: String = wpntq_text(toks, p + 2);
    p = p + 4;
    if p < n && wpntq_kind(toks, p) == "SEMI" { p = p + 1; }
    let mut out: List[String] = list_new();
    out = list_push(out, lit);
    out = list_push(out, p.to_string());
    return out;
}

pub fn wasm_fn_if_paren_not_eq_noelse(src: String, fname: String) -> String {
    if chars_len(src) <= 0 || chars_len(fname) <= 0 { return ""; }
    let toks: List[String] = tokenize_lines(src);
    let n: Int = list_len(toks);
    let mut i: Int = 0;
    while i + 1 < n {
        if wpntq_kind(toks, i) == "KW_FN" && wpntq_kind(toks, i + 1) == "IDENT" {
            if wpntq_text(toks, i + 1) == fname {
                let mut p: Int = i + 2;
                while p < n && wpntq_kind(toks, p) != "LBRACE" { p = p + 1; }
                if p >= n { return ""; }
                p = p + 1;
                if p + 9 >= n || wpntq_kind(toks, p) != "KW_IF" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "LPAREN" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "BANG" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "LPAREN" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "INT" { return ""; }
                let left: Int = parse_int(wpntq_text(toks, p));
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "EQEQ" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "INT" { return ""; }
                let right: Int = parse_int(wpntq_text(toks, p));
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "RPAREN" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "RPAREN" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "LBRACE" { return ""; }
                p = p + 1;
                let th: List[String] = wpntq_one_str_println(toks, p, n);
                if list_len(th) < 2 { return ""; }
                p = parse_int(list_get(th, 1));
                if p >= n || wpntq_kind(toks, p) != "RBRACE" { return ""; }
                p = p + 1;
                if p >= n || wpntq_kind(toks, p) != "RBRACE" { return ""; }
                if left != right { return list_get(th, 0); }
                return "";
            }
        }
        i = i + 1;
    }
    return "";
}
