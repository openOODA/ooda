// # ooda-pm file-backed entry
//
// Logline: File-backed vendor only. Not a fetch of registry.openooda.org.
//
// Setup: Product path is ooda add / ooda pkg / ooda pm on the CLI.
//
// Beats:
//   1. Print that the manager is file-backed.
//   2. Refuse to name a live HTTP registry as product.
//   3. Exit 0.

pub fn main() {
    println("ooda-pm: file-backed vendor (use ooda add / ooda pkg)");
}
