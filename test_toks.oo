pub fn main() {
    let toks = tokenize_lines("pub fn foo(s: String) -> String {}");
    let mut i = 0;
    while i < list_len(toks) {
        println(field_at(list_get(toks, i), 0) + " " + field_at(list_get(toks, i), 3));
        i = i + 1;
    }
}
