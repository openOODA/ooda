pub fn main() {
    let tok = "ARROW\t1\t14\t->";
    let c = char_at(tok, 5);
    if c == "\t" {
        println("Tab matched!");
    } else {
        println("No match, char is: [" + c + "]");
    }
}
