# openOODA v0.168.0-alpha

Shipper: **Grok 4.5 (xAI)**

## Act
R1 oodac: zero-arg call types flow into **call args** (`f(g())`, `let x=g(); f(x)`),
**binops** (`g() + "a"`), and **logic** (`g() && true`).

## E-M
D↓ stage-0 parity for silent-OK call-expression classes. W flat (ret-table + lit-env).

## Not claimed
Full type env / fields/methods / zero-Rust beta.
