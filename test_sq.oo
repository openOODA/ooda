import "oodac/diag_json.oo";
fn main(fs: &FsCap, sys: &SysCap, args: List[String]) {
    let test = "my'file";
    let sq = shell_sq(test);
    sys_exec(sys, "sh", "-c", "printf '>>%s<<\n' \"$0\"", sq);
}
