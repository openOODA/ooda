// # Patched Conics
//
// Logline: Expose a destubbed Int placeholder for patched-conic body count.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. patched_bodies: two-body minimum for patched-conic propagation.
//
// residual: emit-c destub; not the full algorithm.

pub fn patched_bodies() -> Int {
    return 2;
}
