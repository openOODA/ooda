// # Post-Newtonian Ephemeris
//
// Logline: Expose a destubbed Int placeholder for post-Newtonian order.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. pn_order: PN expansion order used by the ephemeris.
//
// residual: emit-c destub; not the full algorithm.

pub fn pn_order() -> Int {
    return 2;
}
