// # Aerobraking Drag Pass
//
// Logline: Expose a destubbed Int constant for aerobraking drag coefficient bookkeeping.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. aero_drag_cd: fixed drag-coefficient scale used by the aerobraking planner.
//
// residual: emit-c destub; not the full algorithm.

pub fn aero_drag_cd() -> Int {
    return 20;
}
