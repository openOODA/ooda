// # Stage Separation Dynamics
//
// Logline: Expose a destubbed Int placeholder for stage-separation delta-v.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. sep_delta_v: nominal separation delta-v in m/s.
//
// residual: emit-c destub; not the full algorithm.

pub fn sep_delta_v() -> Int {
    return 5;
}
