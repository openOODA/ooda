// # Earth-Moon Translunar
//
// Logline: Expose a destubbed Int placeholder for translunar injection delta-v.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. tli_delta_v: scalar TLI delta-v budget in m/s.
//
// residual: emit-c destub; not the full algorithm.

pub fn tli_delta_v() -> Int {
    return 3140;
}
