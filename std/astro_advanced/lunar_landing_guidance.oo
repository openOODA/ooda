// # Lunar Landing Guidance
//
// Logline: Expose a destubbed Int placeholder for lunar landing delta-v.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. lunar_land_dv: nominal landing delta-v in m/s.
//
// residual: emit-c destub; not the full algorithm.

pub fn lunar_land_dv() -> Int {
    return 2000;
}
