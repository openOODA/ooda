// # Lunar Orbit Insertion
//
// Logline: Expose a destubbed Int placeholder for lunar orbit insertion delta-v.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. loi_dv: nominal LOI delta-v in m/s.
//
// residual: emit-c destub; not the full algorithm.

pub fn loi_dv() -> Int {
    return 830;
}
