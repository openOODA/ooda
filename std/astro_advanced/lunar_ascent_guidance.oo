// # Lunar Ascent Guidance
//
// Logline: Expose a destubbed Int placeholder for lunar ascent delta-v.
//
// Setup: Pure-emit destub. Single pub fn returns an Int constant derived from the file's domain. CWD must be ooda/.
//
// Beats:
//   1. lunar_ascent_dv: nominal ascent delta-v in m/s.
//
// residual: emit-c destub; not the full algorithm.

pub fn lunar_ascent_dv() -> Int {
    return 1870;
}
