// # Standard library module
//
// Logline: A small public surface with a one-line job and honest residuals.
//
// Setup: Pure unless a function takes a &Cap. Callers import this file.
//
// Beats:
//   1. Header says what the file does.
//   2. Each public function has a one-line comment.
//   3. Residual lines name what is not built.

// residual: name anything this file looks like it does but does not

// One-line: what this function returns.
pub fn template_op(arg: String) -> String {
    if arg == "" { return "error: empty input"; }
    return arg;
}

pub fn template_helper(arg: String) -> String {
    return arg;
}

