// # <module-name>
//
// Purpose: <one-line: pure / impure surface>
// Caps:    <none | comma list of &CapName>

// ## CAPs
//   ProcessCap:    run child helpers (only if main spawns)

// ## Tokenomics
//   canonical:    yes

// ## <verb> — <one-line summary>
// ## pre:  <precondition>
// ## post: <postcondition>
pub fn <name>(args) -> Ret {
    if <cond> { return <err>; }
    return <ok>;
}

// ## <verb> — <summary>
// ## pre:  <input shape>
// ## post: <output shape>
pub fn <helper>(args) -> Ret {
    return <value>;
}