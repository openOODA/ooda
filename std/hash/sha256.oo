// package: std::hash
// job: residual leaf only — not a second SHA-256
// residual: product sha256 lives in std/core/crypto.oo (CHS, not FIPS). This leaf is not a hash.
// S: 0 — this leaf has no product hash (F38b)

// Check requires at least one fn. Unused fail-closed stub (not named sha256).
fn hash_sha256_leaf_unused() {
    process_exit(1);
}
