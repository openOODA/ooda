// # Unused Hash Leaf
//
// Logline: This file is not a hash; check needs one function and that function exits.
//
// Setup: Product SHA-256 lives in std/core/crypto.oo (CHS, not FIPS). This leaf must not declare sha256.
//
// Beats:
//   1. Publish one unused private function so check sees a body.
//   2. The name is hash_sha256_leaf_unused, not sha256.
//   3. The stub calls process_exit(1) and returns nothing.
//
// residual: unused fail-closed stub; no digest; product sha256 is std/core/crypto.oo

// Check requires at least one fn. Unused fail-closed stub (not named sha256).
fn hash_sha256_leaf_unused() {
    process_exit(1);
}
