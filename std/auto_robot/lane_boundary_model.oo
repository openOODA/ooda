// # Lane linear offset
//
// Logline: Integer first-order lane offset y = c0 + c1 * x.
// residual: destubbed to Int floor.

pub fn lane_lin_y(c0: Int, c1: Int, x: Int) -> Int {
    return c0 + (c1 * x);
}
