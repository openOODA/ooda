// # Time-to-collision quotient
//
// Logline: Integer TTC as distance over closing speed.
// residual: destubbed to Int floor.

pub fn ttc_quot(dist: Int, closing: Int) -> Int {
    return dist / closing;
}
