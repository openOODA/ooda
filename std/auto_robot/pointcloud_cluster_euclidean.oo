// # Euclidean cluster distance square
//
// Logline: Integer squared planar distance of a cluster offset.
// residual: destubbed to Int floor.

pub fn euc_dist_sq(dx: Int, dy: Int) -> Int {
    return (dx * dx) + (dy * dy);
}
