// # MPC cross-track error square
//
// Logline: Integer squared position error dx*dx + dy*dy.
// residual: destubbed to Int floor.

pub fn mpc_cte_sq(dx: Int, dy: Int) -> Int {
    return (dx * dx) + (dy * dy);
}
