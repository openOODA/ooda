// # Particle mean x
//
// Logline: Integer weighted mean of two particle x positions.
// residual: destubbed to Int floor.

pub fn pf_mean_x(x1: Int, w1: Int, x2: Int, w2: Int) -> Int {
    return ((x1 * w1) + (x2 * w2)) / (w1 + w2);
}
