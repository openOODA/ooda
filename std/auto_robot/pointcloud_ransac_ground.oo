// # RANSAC plane evaluate
//
// Logline: Integer plane residual a*x + b*y + c*z + d.
// residual: destubbed to Int floor.

pub fn ransac_plane_eval(a: Int, b: Int, c: Int, d: Int, x: Int, y: Int, z: Int) -> Int {
    return (((a * x) + (b * y)) + (c * z)) + d;
}
