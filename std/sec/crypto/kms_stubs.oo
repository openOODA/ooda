// # KMS Fail-Closed Stubs
//
// Logline: Enforces fail-closed process exit barriers for unverified cryptographic operations in KMS.
//
// Setup: Imports companion modules for fail-closed stub declarations.
//
// Beats:
//   1. Import KMS engine and state types.
//   2. Trap unverified low-level primitives with process_exit(1).
//   3. Guard unaudited hardware acceleration and FFI routines.

import "./kms_engine.oo";

pub fn kms_module_ready() -> Bool {
    return true;
}

pub fn kms_hsm_generate_master_key(_key_id: String, _key_type: String) -> Int {
    process_exit(1);
    return 0;
}

pub fn kms_hsm_derive_dek(_master_key: &KmsKey, _context: String) -> Int {
    process_exit(1);
    return 0;
}

pub fn kms_envelope_encrypt_dek(_master_key: &KmsKey, _plaintext_dek: String) -> Int {
    process_exit(1);
    return 0;
}

pub fn kms_envelope_decrypt_dek(_master_key: &KmsKey, _envelope: &KeyEnvelope) -> Int {
    process_exit(1);
    return 0;
}

pub fn kms_cloud_kms_reencrypt(
    _source_key: &KmsKey,
    _target_key: &KmsKey,
    _envelope: &KeyEnvelope
) -> Int {
    process_exit(1);
    return 0;
}

pub fn kms_post_quantum_wrap(_master_key: &KmsKey, _raw_dek: String) -> Int {
    process_exit(1);
    return 0;
}
