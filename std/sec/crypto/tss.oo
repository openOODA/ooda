// # Threshold Signature Scheme Facade
//
// Logline: Re-exports TSS and FROST data types, operational constructors, engine lifecycle, and fail-closed stubs.
//
// Setup: Pure module shim importing companion submodules.
//
// Beats:
//   1. Import TSS companion modules.
//   2. Re-export public symbols with typed shim markers.
//   3. Maintain compatibility with downstream threshold cryptography consumers.

import "./tss_types.oo";
import "./tss_ops.oo";

pub fn shim_marker_tss_params() -> ThresholdParams {
    return tss_params_new(2, 3, 1);
}

pub fn shim_marker_tss_ops_init(threshold: Int, total: Int, curve: Int) -> ThresholdParams {
    return tss_ops_init(threshold, total, curve);
}

pub fn shim_marker_tss_params_t(p: ThresholdParams) -> Int {
    return tss_params_t(p);
}

pub fn __dummy_std_src_ooda_sec_crypto_tss_oo() -> Int {
    return 0;
}
