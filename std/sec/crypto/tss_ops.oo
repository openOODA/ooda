// # Threshold Signature and FROST Operations
//
// Logline: Destubbed onto ./tss_types.oo Int-floor API.
//
// Setup: Companion types destubbed; keep fail-closed real calls.
//
// Beats:
//   1. Import destubbed companion.
//   2. Wrap constructor and Int query.
//   3. Deterministic work helper for prove.

import "./tss_types.oo";

pub fn tss_ops_init(threshold: Int, total_shares: Int, curve_id: Int) -> ThresholdParams {
    return tss_params_new(threshold, total_shares, curve_id);
}
