// # Attestation types
//
// Logline: AttestationPolicy destubbed off List[AttestationMeasurement].
// residual: List[struct] destubbed.

type AttestationPolicy = struct {
    min_trust_score: Int,
    allowed_pcr_mask: Int
};

pub fn attestation_policy_new(min_trust_score: Int, allowed_pcr_mask: Int) -> AttestationPolicy {
    return AttestationPolicy {
        min_trust_score: min_trust_score,
        allowed_pcr_mask: allowed_pcr_mask
    };
}

pub fn attestation_policy_score(p: AttestationPolicy) -> Int {
    return p.min_trust_score;
}
