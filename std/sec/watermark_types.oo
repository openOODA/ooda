// # Watermark types
//
// Logline: WatermarkMeta destubbed off nested policy lists.
// residual: List[struct] destubbed.

type WatermarkMeta = struct {
    algo_id: Int,
    strength: Int
};

pub fn watermark_meta_new(algo_id: Int, strength: Int) -> WatermarkMeta {
    return WatermarkMeta {
        algo_id: algo_id,
        strength: strength
    };
}

pub fn watermark_meta_algo(m: WatermarkMeta) -> Int {
    return m.algo_id;
}
