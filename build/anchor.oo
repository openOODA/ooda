// # Ooda Router Helpers — Anchor for Agents
//
// Logline: Front door for the ooda/ router helpers. spawn resolves and execs external binaries (cli, opm, lsp, mcp).
//
// Setup: spawn() runs external tools via ProcessCap. Phase 8 router trim: only spawn.oo remains; all 9 deprecated handlers (bench, context, eval, gen, health, repl, sandbox, swarm, digest) moved to cli/, tui/, or blackbox/.
//
// Beats:
//   1. spawn — resolves and execs cli/opm/lsp/mcp under OODACODEX/bin or ~/.openooda/bin.

import "spawn.oo";
